---
phase: 11-strapi-schema-migration-localization-helper
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cms/src/api/product-category/content-types/product-category/schema.json
  - cms/src/api/product-category/routes/product-category.js
  - cms/src/api/product-category/controllers/product-category.js
  - cms/src/api/product-category/services/product-category.js
  - cms/src/api/recipe-collection/content-types/recipe-collection/schema.json
  - cms/src/api/recipe-collection/routes/recipe-collection.js
  - cms/src/api/recipe-collection/controllers/recipe-collection.js
  - cms/src/api/recipe-collection/services/recipe-collection.js
  - cms/src/api/product/content-types/product/schema.json
  - cms/src/api/subscriber/content-types/subscriber/schema.json
  - cms/src/api/recipe/content-types/recipe/schema.json
autonomous: true
requirements:
  - FILT-02
  - NEWS-05
  - COLL-06

must_haves:
  truths:
    - "product-category content type exists in Strapi with localized name, slug, and description fields"
    - "Product schema has product_category manyToOne relation and price decimal field, no category enum, no price_range enum"
    - "recipe-collection content type exists with title, slug, hero_image, editorial_intro, localized EN/IT/ES"
    - "Recipe schema has collection manyToOne relation to recipe-collection"
    - "Subscriber schema has source enum with 5 values"
  artifacts:
    - path: "cms/src/api/product-category/content-types/product-category/schema.json"
      provides: "Product category taxonomy content type"
      contains: "product-category"
    - path: "cms/src/api/recipe-collection/content-types/recipe-collection/schema.json"
      provides: "Recipe collection content type"
      contains: "recipe-collection"
    - path: "cms/src/api/product/content-types/product/schema.json"
      provides: "Updated Product with relation and decimal price"
      contains: "product_category"
    - path: "cms/src/api/subscriber/content-types/subscriber/schema.json"
      provides: "Subscriber with source enum"
      contains: "source"
    - path: "cms/src/api/recipe/content-types/recipe/schema.json"
      provides: "Recipe with collection relation"
      contains: "collection"
  key_links:
    - from: "cms/src/api/product/content-types/product/schema.json"
      to: "cms/src/api/product-category/content-types/product-category/schema.json"
      via: "manyToOne relation target"
      pattern: "api::product-category.product-category"
    - from: "cms/src/api/recipe/content-types/recipe/schema.json"
      to: "cms/src/api/recipe-collection/content-types/recipe-collection/schema.json"
      via: "manyToOne relation target"
      pattern: "api::recipe-collection.recipe-collection"
---

<objective>
Create all new Strapi content types (product-category, recipe-collection) and modify existing schemas (Product, Subscriber, Recipe) to land the full v1.1 schema in one shot.

Purpose: All downstream phases (12-14) depend on these schema changes being present in Strapi. This plan creates the schema files locally; deployment happens in Plan 03.
Output: 2 new content types + 3 modified schemas ready for Strapi rebuild.
</objective>

<execution_context>
@C:/Users/Matteo/Desktop/Progetti/bbqexperience/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Matteo/Desktop/Progetti/bbqexperience/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-strapi-schema-migration-localization-helper/11-CONTEXT.md

<interfaces>
<!-- Strapi v5 content type pattern — replicate Brand structure for new types -->

From cms/src/api/brand/content-types/brand/schema.json:
```json
{
  "kind": "collectionType",
  "collectionName": "brands",
  "info": { "singularName": "brand", "pluralName": "brands", "displayName": "Brand" },
  "options": { "draftAndPublish": false },
  "pluginOptions": { "i18n": { "localized": false } },
  "attributes": { "name": { "type": "string", "required": true, "unique": true } }
}
```

From cms/src/api/product/content-types/product/schema.json (current — to be modified):
```json
{
  "attributes": {
    "category": { "type": "enumeration", "enum": ["grill","smoker","accessory","fuel","thermometer","other"] },
    "price_range": { "type": "enumeration", "enum": ["budget","mid-range","premium","luxury"] },
    "brand_relation": { "type": "relation", "relation": "manyToOne", "target": "api::brand.brand" }
  }
}
```

Relation pattern (from brand_relation on Product):
```json
"brand_relation": {
  "type": "relation",
  "relation": "manyToOne",
  "target": "api::brand.brand"
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create product-category and recipe-collection content types</name>
  <files>
    cms/src/api/product-category/content-types/product-category/schema.json,
    cms/src/api/product-category/routes/product-category.js,
    cms/src/api/product-category/controllers/product-category.js,
    cms/src/api/product-category/services/product-category.js,
    cms/src/api/recipe-collection/content-types/recipe-collection/schema.json,
    cms/src/api/recipe-collection/routes/recipe-collection.js,
    cms/src/api/recipe-collection/controllers/recipe-collection.js,
    cms/src/api/recipe-collection/services/recipe-collection.js
  </files>
  <read_first>
    cms/src/api/brand/content-types/brand/schema.json,
    cms/src/api/brand/routes/brand.js,
    cms/src/api/brand/controllers/brand.js,
    cms/src/api/brand/services/brand.js
  </read_first>
  <action>
Create two new Strapi v5 content types following the exact pattern of the Brand content type (routes/controllers/services boilerplate + schema.json).

**product-category** (per D-01, D-02):
- `kind`: "collectionType", `collectionName`: "product_categories"
- `singularName`: "product-category", `pluralName`: "product-categories", `displayName`: "Product Category"
- `description`: "Categorie prodotto BBQ (grill, smoker, pellet, ecc.)"
- `draftAndPublish`: false (utility type, no draft workflow)
- `i18n.localized`: true (the content type itself is localized)
- Attributes:
  - `name`: type "string", required true, pluginOptions i18n.localized true
  - `slug`: type "uid", targetField "name"
  - `description`: type "text", pluginOptions i18n.localized true

**recipe-collection** (per D-08):
- `kind`: "collectionType", `collectionName`: "recipe_collections"
- `singularName`: "recipe-collection", `pluralName`: "recipe-collections", `displayName`: "Recipe Collection"
- `description`: "Raccolte tematiche di ricette BBQ"
- `draftAndPublish`: true (editorial content, needs draft workflow)
- `i18n.localized`: true
- Attributes:
  - `title`: type "string", required true, pluginOptions i18n.localized true
  - `slug`: type "uid", targetField "title"
  - `hero_image`: type "media", multiple false, allowedTypes ["images"]
  - `editorial_intro`: type "richtext", pluginOptions i18n.localized true
  - `order`: type "integer", default 0 (per ordinamento manuale nella lista)
  - `recipes`: type "relation", relation "oneToMany", target "api::recipe.recipe", mappedBy "collection"

Routes/controllers/services: use the standard Strapi v5 boilerplate (module.exports with createCoreRouter/createCoreController/createCoreService pattern, copied from Brand).
  </action>
  <verify>
    <automated>node -e "const pc=require('./cms/src/api/product-category/content-types/product-category/schema.json'); const rc=require('./cms/src/api/recipe-collection/content-types/recipe-collection/schema.json'); console.log('pc-name:', pc.attributes.name.type, 'pc-slug:', pc.attributes.slug.type, 'pc-i18n:', pc.pluginOptions.i18n.localized); console.log('rc-title:', rc.attributes.title.type, 'rc-hero:', rc.attributes.hero_image.type, 'rc-recipes:', rc.attributes.recipes.relation); process.exit(pc.attributes.name.type==='string' && rc.attributes.recipes.relation==='oneToMany' ? 0 : 1)"</automated>
  </verify>
  <acceptance_criteria>
    - `cms/src/api/product-category/content-types/product-category/schema.json` exists with `"singularName": "product-category"`
    - product-category has `"pluginOptions": { "i18n": { "localized": true } }` at root level
    - product-category has `name` (string, required, localized), `slug` (uid), `description` (text, localized)
    - `cms/src/api/recipe-collection/content-types/recipe-collection/schema.json` exists with `"draftAndPublish": true`
    - recipe-collection has `title`, `slug`, `hero_image`, `editorial_intro`, `order`, `recipes` attributes
    - recipe-collection `recipes` relation is `"oneToMany"` targeting `"api::recipe.recipe"` with `"mappedBy": "collection"`
    - All 4 boilerplate files exist for each type (schema.json, routes, controllers, services)
  </acceptance_criteria>
  <done>Two new content types with full Strapi v5 structure ready for build</done>
</task>

<task type="auto">
  <name>Task 2: Modify Product, Subscriber, and Recipe schemas</name>
  <files>
    cms/src/api/product/content-types/product/schema.json,
    cms/src/api/subscriber/content-types/subscriber/schema.json,
    cms/src/api/recipe/content-types/recipe/schema.json
  </files>
  <read_first>
    cms/src/api/product/content-types/product/schema.json,
    cms/src/api/subscriber/content-types/subscriber/schema.json,
    cms/src/api/recipe/content-types/recipe/schema.json
  </read_first>
  <action>
Modify three existing Strapi content type schemas:

**Product** (per D-01, D-04, D-05):
1. REMOVE the `category` attribute entirely (the enumeration with grill/smoker/accessory/fuel/thermometer/other)
2. REMOVE the `price_range` attribute entirely (the enumeration with budget/mid-range/premium/luxury)
3. ADD `product_category` relation:
   ```json
   "product_category": {
     "type": "relation",
     "relation": "manyToOne",
     "target": "api::product-category.product-category"
   }
   ```
4. ADD `price` decimal field:
   ```json
   "price": {
     "type": "decimal"
   }
   ```
Place `product_category` where `category` was, and `price` where `price_range` was, to keep the schema readable.

**Subscriber** (per D-06):
ADD `source` enum field after `unsubscribed_at`:
```json
"source": {
  "type": "enumeration",
  "enum": ["inline", "landing", "footer", "exit-intent", "legacy"],
  "default": "legacy"
}
```
Default is "legacy" so any existing rows get the right value. Subscriber is NOT localized (i18n.localized: false), no pluginOptions needed on the field.

**Recipe** (per D-09):
ADD `collection` relation after `published_date`:
```json
"collection": {
  "type": "relation",
  "relation": "manyToOne",
  "target": "api::recipe-collection.recipe-collection",
  "inversedBy": "recipes"
}
```
  </action>
  <verify>
    <automated>node -e "const p=require('./cms/src/api/product/content-types/product/schema.json'); const s=require('./cms/src/api/subscriber/content-types/subscriber/schema.json'); const r=require('./cms/src/api/recipe/content-types/recipe/schema.json'); const ok = !p.attributes.category && !p.attributes.price_range && p.attributes.product_category && p.attributes.price && s.attributes.source && r.attributes.collection; console.log('product_category:', !!p.attributes.product_category, 'price:', p.attributes.price?.type, 'no-category:', !p.attributes.category, 'source:', s.attributes.source?.enum?.length, 'collection:', r.attributes.collection?.relation); process.exit(ok ? 0 : 1)"</automated>
  </verify>
  <acceptance_criteria>
    - Product schema has NO `category` attribute (grep for `"category"` should NOT match an enumeration attribute)
    - Product schema has NO `price_range` attribute
    - Product schema has `product_category` with `"relation": "manyToOne"` and `"target": "api::product-category.product-category"`
    - Product schema has `price` with `"type": "decimal"`
    - Subscriber schema has `source` with enum `["inline", "landing", "footer", "exit-intent", "legacy"]` and default `"legacy"`
    - Recipe schema has `collection` with `"relation": "manyToOne"` and `"target": "api::recipe-collection.recipe-collection"` and `"inversedBy": "recipes"`
    - All three JSON files are valid JSON (node -e require succeeds)
  </acceptance_criteria>
  <done>Product, Subscriber, and Recipe schemas updated with all v1.1 fields; old enums removed</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Schema files → Strapi build | Schema JSON is loaded by Strapi at build time; malformed JSON breaks the CMS |
| Strapi API → Frontend | New relations change API response shape; downstream consumers must populate correctly |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | T (Tampering) | schema.json files | mitigate | Validate JSON syntax with node -e require() before commit; Strapi build will reject invalid schemas |
| T-11-02 | D (Denial of Service) | product-category API | accept | New content type has default Strapi permissions (no public access until explicitly granted); low risk |
| T-11-03 | I (Information Disclosure) | recipe-collection API | mitigate | Verify default permissions are restrictive (Strapi v5 defaults to no public access); grant only find/findOne in Phase 14 |
</threat_model>

<verification>
- `node -e "require('./cms/src/api/product-category/content-types/product-category/schema.json')"` exits 0
- `node -e "require('./cms/src/api/recipe-collection/content-types/recipe-collection/schema.json')"` exits 0
- `node -e "require('./cms/src/api/product/content-types/product/schema.json')"` exits 0
- Product schema contains `product_category` relation and `price` decimal, no `category` enum, no `price_range` enum
- Subscriber schema contains `source` enum with 5 values
- Recipe schema contains `collection` relation to recipe-collection
</verification>

<success_criteria>
All 5 schema files (2 new + 3 modified) are valid JSON, contain the correct attributes per decisions D-01 through D-09, and are ready for Strapi build.
</success_criteria>

<output>
After completion, create `.planning/phases/11-strapi-schema-migration-localization-helper/11-01-SUMMARY.md`
</output>
