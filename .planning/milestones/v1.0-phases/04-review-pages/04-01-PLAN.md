---
phase: 04-review-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/components/review/ScoreCard.astro
  - web/src/components/review/SpecsTable.astro
  - web/src/components/review/ProsConsCard.astro
  - web/src/components/review/VerdictCard.astro
  - web/src/components/review/ReviewJsonLd.astro
  - web/src/pages/en/reviews/[slug].astro
  - web/src/pages/it/recensioni/[slug].astro
  - web/src/pages/es/resenas/[slug].astro
  - web/src/i18n/en.json
  - web/src/i18n/it.json
  - web/src/i18n/es.json
autonomous: true
requirements:
  - REV-01
  - REV-02
  - REV-03
  - REV-05
  - REV-08

must_haves:
  truths:
    - "User sees overall score and 4 per-category scores on review page"
    - "User reads long-form editorial content rendered from rich text"
    - "User sees a structured specs table with product specifications"
    - "User sees pros and cons lists clearly separated"
    - "User sees a condensed verdict card with score, verdict text, pros/cons, and product photo"
    - "Review page outputs valid Product + Review + AggregateRating JSON-LD"
  artifacts:
    - path: "web/src/components/review/ScoreCard.astro"
      provides: "Overall score display + 4 category score bars"
      min_lines: 40
    - path: "web/src/components/review/SpecsTable.astro"
      provides: "Technical specifications key-value table"
      min_lines: 20
    - path: "web/src/components/review/ProsConsCard.astro"
      provides: "Pros and cons side-by-side display"
      min_lines: 25
    - path: "web/src/components/review/VerdictCard.astro"
      provides: "Condensed verdict summary card"
      min_lines: 30
    - path: "web/src/components/review/ReviewJsonLd.astro"
      provides: "Schema.org JSON-LD for Product + Review + AggregateRating"
      min_lines: 30
  key_links:
    - from: "web/src/pages/en/reviews/[slug].astro"
      to: "web/src/components/review/ScoreCard.astro"
      via: "Astro component import"
      pattern: "import ScoreCard"
    - from: "web/src/pages/en/reviews/[slug].astro"
      to: "web/src/components/review/ReviewJsonLd.astro"
      via: "Astro component import"
      pattern: "import ReviewJsonLd"
    - from: "web/src/components/review/ReviewJsonLd.astro"
      to: "Schema.org"
      via: "JSON-LD script tag"
      pattern: "application/ld\\+json"
---

<objective>
Build all review page components (scoring, specs, pros/cons, verdict card) and Schema.org structured data, then replace the skeleton review pages with the full review experience in all 3 locales.

Purpose: Delivers the core review reading experience — users see structured scores, editorial content, specs, and verdict summaries. Schema.org enables rich search results.
Output: 5 new Astro components in web/src/components/review/, 3 updated locale pages, updated i18n strings.
</objective>

<execution_context>
@C:/Users/Matteo/Desktop/Progetti/bbqexperience/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Matteo/Desktop/Progetti/bbqexperience/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From web/src/lib/types.ts:
```typescript
export interface StrapiProduct {
  name: string;
  slug: string;
  brand: string | null;
  category: 'grill' | 'smoker' | 'accessory' | 'fuel' | 'thermometer' | 'other' | null;
  price_range: 'budget' | 'mid-range' | 'premium' | 'luxury' | null;
  description: string | null;
  specifications: Record<string, unknown> | null;
  images: StrapiMedia[] | null;
  affiliate_url: string | null;
}

export interface StrapiReview {
  title: string;
  slug: string;
  excerpt: string | null;
  editorial_content: string | null;
  verdict: string | null;
  score_overall: number;
  score_build_quality: number | null;
  score_performance: number | null;
  score_value: number | null;
  score_ease_of_use: number | null;
  pros: string[] | null;
  cons: string[] | null;
  product: (StrapiProduct & StrapiEntity) | null;
  gallery: StrapiMedia[] | null;
  seo_title: string | null;
  seo_description: string | null;
  published_date: string | null;
}
```

From web/src/lib/media.ts:
```typescript
export function getStrapiMediaURL(media: StrapiMedia | null | undefined): string | null;
export function getStrapiImageFormats(media: StrapiMedia | null | undefined): { thumbnail?: string; small?: string; medium?: string; large?: string };
export function getStrapiMediaAlt(media: StrapiMedia | null | undefined): string;
```

From web/src/lib/i18n.ts:
```typescript
export type Locale = 'en' | 'it' | 'es';
export async function loadTranslations(locale: Locale): Promise<Record<string, any>>;
export function getTranslation(translations: Record<string, any>, key: string): string;
```

From web/src/lib/preview.ts:
```typescript
export function getContentStatus(cookies: AstroCookies): 'published' | 'draft';
```

From web/src/styles/tokens.css:
```css
--color-bg-primary: #0D0D0D;
--color-bg-secondary: #1A1A1A;
--color-bg-tertiary: #262626;
--color-bg-elevated: #2D2D2D;
--color-accent-fire: #F97316;
--color-accent-amber: #F59E0B;
--color-accent-ember: #EA580C;
--color-accent-smoke: #78716C;
--color-text-primary: #F5F5F4;
--color-text-secondary: #D6D3D1;
--color-text-muted: #A8A29E;
--color-border-default: #404040;
--color-border-subtle: #2D2D2D;
--font-heading: 'Oswald Variable', 'Impact', sans-serif;
--font-body: 'Inter Variable', system-ui, sans-serif;
--shadow-glow-fire: 0 0 20px rgba(249, 115, 22, 0.3);
```

Existing review i18n keys in en.json:
```json
"review": {
  "overallScore": "Overall Score",
  "buildQuality": "Build Quality",
  "performance": "Performance",
  "value": "Value for Money",
  "easeOfUse": "Ease of Use",
  "verdict": "Our Verdict",
  "pros": "Pros",
  "cons": "Cons",
  "specs": "Specifications"
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create review display components (ScoreCard, SpecsTable, ProsConsCard, VerdictCard)</name>
  <files>
    web/src/components/review/ScoreCard.astro
    web/src/components/review/SpecsTable.astro
    web/src/components/review/ProsConsCard.astro
    web/src/components/review/VerdictCard.astro
  </files>
  <action>
    read_first:
    - web/src/styles/tokens.css (design tokens)
    - web/src/lib/types.ts (StrapiReview, StrapiProduct interfaces)
    - web/src/lib/media.ts (getStrapiMediaURL, getStrapiImageFormats)
    - web/src/components/common/Hero.astro (reference for existing component patterns)
    - web/src/i18n/en.json (existing review translation keys)

    Create 4 Astro components in web/src/components/review/:

    **ScoreCard.astro** — Displays overall score + 4 category scores.
    Props: `{ overall: number; buildQuality: number | null; performance: number | null; value: number | null; easeOfUse: number | null; translations: Record<string, any> }`
    Layout:
    - Large circular overall score at top: `div` with `w-28 h-28 rounded-full` centered, border-4 using `var(--color-accent-fire)`, score number in `font-[var(--font-heading)] text-5xl` text color `var(--color-accent-amber)`, "/10" below in `var(--color-text-muted)`
    - Below: 4 horizontal category bars in a grid (2 cols on md+, 1 col on mobile)
    - Each category bar: label on left (from translations using `getTranslation(translations, 'review.buildQuality')` etc.), score number on right, and a horizontal bar fill (CSS width percentage = score/10*100%) with `bg-[var(--color-accent-fire)]` on `bg-[var(--color-bg-tertiary)]` track, height 8px, rounded
    - Skip any category where score is null
    - Wrap entire component in `section` with `bg-[var(--color-bg-secondary)] rounded-[var(--radius-lg)] p-6 md:p-8` and `data-animate` attribute

    **SpecsTable.astro** — Renders product specifications as a key-value table.
    Props: `{ specifications: Record<string, unknown> | null; title: string }`
    Layout:
    - Section heading using `font-[var(--font-heading)] text-2xl uppercase tracking-wider text-[var(--color-text-primary)]`
    - Table with alternating row backgrounds: odd rows `bg-[var(--color-bg-secondary)]`, even rows `bg-[var(--color-bg-tertiary)]`
    - Each row: key cell (capitalize, `text-[var(--color-text-muted)]` font-medium, w-1/3), value cell (`text-[var(--color-text-primary)]`)
    - Use `Object.entries(specifications)` to iterate. If specifications is null or empty, render nothing (return empty fragment).
    - Table uses `w-full border-collapse`, cells have `px-4 py-3`
    - Wrap in `data-animate`

    **ProsConsCard.astro** — Shows pros and cons side-by-side.
    Props: `{ pros: string[] | null; cons: string[] | null; translations: Record<string, any> }`
    Layout:
    - 2-column grid on md+ (`grid md:grid-cols-2 gap-6`), stacked on mobile
    - Pros column: heading with green check icon (inline SVG checkmark, `text-green-500`), `getTranslation(translations, 'review.pros')` as title. Each pro item as `li` with `text-[var(--color-text-secondary)]` and a small green dot bullet
    - Cons column: heading with red X icon (inline SVG x-mark, `text-red-500`), `getTranslation(translations, 'review.cons')` as title. Each con item as `li` with `text-[var(--color-text-secondary)]` and a small red dot bullet
    - Background: `bg-[var(--color-bg-secondary)] rounded-[var(--radius-lg)] p-6 md:p-8`
    - If both pros and cons are null/empty, render nothing
    - Wrap in `data-animate`

    **VerdictCard.astro** — Condensed review summary card.
    Props: `{ overall: number; verdict: string | null; pros: string[] | null; cons: string[] | null; productName: string; productImage: string | null; productImageAlt: string; translations: Record<string, any> }`
    Layout:
    - Full-width card with `bg-[var(--color-bg-elevated)] rounded-[var(--radius-xl)] p-6 md:p-8` and `border border-[var(--color-border-accent)]` and `shadow-[var(--shadow-glow-fire)]`
    - Flex row on md+ (image left, content right), stacked on mobile
    - Left side (md:w-1/3): product image if available, `rounded-[var(--radius-lg)] object-cover aspect-square`, fallback to a placeholder div with `bg-[var(--color-bg-tertiary)]` and product name initial
    - Right side (md:w-2/3):
      - Product name in `font-[var(--font-heading)] text-xl uppercase`
      - Score badge: inline `span` with `bg-[var(--color-accent-fire)] text-[var(--color-text-inverse)] font-bold rounded-[var(--radius-md)] px-3 py-1 text-lg`
      - Verdict text in `text-[var(--color-text-secondary)] italic text-lg mt-3`
      - Compact pros/cons: first 2 pros and first 2 cons only, as single-line items with green/red dots
    - Wrap in `data-animate`

    All components: Use design token CSS variables (not hardcoded hex). Use `var(--font-heading)` for headings, `var(--font-body)` or default for body text. Include `data-animate` on the outer wrapper for GSAP scroll animations.
  </action>
  <verify>
    <automated>cd C:/Users/Matteo/Desktop/Progetti/bbqexperience/web && npx astro check 2>&1 | tail -20</automated>
  </verify>
  <done>
    - ScoreCard.astro renders overall score circle + 4 category bars with null handling
    - SpecsTable.astro renders key-value specification table with alternating rows
    - ProsConsCard.astro renders pros/cons in 2-column layout with colored icons
    - VerdictCard.astro renders condensed summary with product image, score badge, verdict, top pros/cons
    - All components use design tokens from tokens.css, not hardcoded colors
    - All components have data-animate for GSAP scroll integration
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Schema.org JSON-LD component and update i18n translation files</name>
  <files>
    web/src/components/review/ReviewJsonLd.astro
    web/src/i18n/en.json
    web/src/i18n/it.json
    web/src/i18n/es.json
  </files>
  <action>
    read_first:
    - web/src/lib/types.ts (StrapiReview, StrapiProduct)
    - web/src/i18n/en.json (current keys)
    - web/src/i18n/it.json (current keys)
    - web/src/i18n/es.json (current keys)

    **ReviewJsonLd.astro** — Outputs Product + Review + AggregateRating structured data (REV-08).
    Props: `{ review: StrapiReview & StrapiEntity; productImageUrl: string | null; locale: string; siteUrl: string }`
    Implementation:
    - Build a JSON-LD object following Schema.org specs:
    ```json
    {
      "@context": "https://schema.org",
      "@type": "Product",
      "name": review.product?.name || review.title,
      "brand": { "@type": "Brand", "name": review.product?.brand || "" },
      "image": productImageUrl || "",
      "description": review.excerpt || "",
      "category": review.product?.category || "",
      "review": {
        "@type": "Review",
        "reviewRating": {
          "@type": "Rating",
          "ratingValue": review.score_overall,
          "bestRating": 10,
          "worstRating": 0
        },
        "author": {
          "@type": "Person",
          "name": "BBQ Experience"
        },
        "reviewBody": review.verdict || "",
        "datePublished": review.published_date || review.publishedAt || ""
      },
      "aggregateRating": {
        "@type": "AggregateRating",
        "ratingValue": review.score_overall,
        "bestRating": 10,
        "worstRating": 0,
        "ratingCount": 1,
        "reviewCount": 1
      }
    }
    ```
    - Render as `<script type="application/ld+json" set:html={JSON.stringify(jsonLd)} />`
    - Omit brand object if brand is null. Omit image if null. Omit category if null.

    **i18n updates** — Add missing review-specific translation keys to all 3 locale files.
    Add these keys under `"review"` in each file:

    en.json additions:
    ```json
    "review": {
      ... (keep existing keys),
      "gallery": "Photo Gallery",
      "verdictCard": "The Verdict",
      "score": "Score",
      "outOf": "out of 10",
      "category": "Category",
      "priceRange": "Price Range",
      "brand": "Brand",
      "publishedOn": "Published on",
      "noSpecs": "No specifications available"
    }
    ```

    it.json additions (same new keys, Italian values):
    ```json
    "gallery": "Galleria Foto",
    "verdictCard": "Il Verdetto",
    "score": "Punteggio",
    "outOf": "su 10",
    "category": "Categoria",
    "priceRange": "Fascia di Prezzo",
    "brand": "Marca",
    "publishedOn": "Pubblicato il",
    "noSpecs": "Nessuna specifica disponibile"
    ```

    es.json additions (same new keys, Spanish values):
    ```json
    "gallery": "Galeria de Fotos",
    "verdictCard": "El Veredicto",
    "score": "Puntuacion",
    "outOf": "de 10",
    "category": "Categoria",
    "priceRange": "Rango de Precio",
    "brand": "Marca",
    "publishedOn": "Publicado el",
    "noSpecs": "No hay especificaciones disponibles"
    ```

    Also translate the existing review keys that are already in en.json into it.json and es.json if not already present:
    - it.json "review": { "overallScore": "Punteggio Generale", "buildQuality": "Qualita Costruttiva", "performance": "Prestazioni", "value": "Rapporto Qualita/Prezzo", "easeOfUse": "Facilita d'Uso", "verdict": "Il Nostro Verdetto", "pros": "Pro", "cons": "Contro", "specs": "Specifiche Tecniche" }
    - es.json "review": { "overallScore": "Puntuacion General", "buildQuality": "Calidad de Construccion", "performance": "Rendimiento", "value": "Relacion Calidad/Precio", "easeOfUse": "Facilidad de Uso", "verdict": "Nuestro Veredicto", "pros": "Ventajas", "cons": "Desventajas", "specs": "Especificaciones" }
  </action>
  <verify>
    <automated>cd C:/Users/Matteo/Desktop/Progetti/bbqexperience/web && npx astro check 2>&1 | tail -20 && node -e "const en=require('./src/i18n/en.json'); const it=require('./src/i18n/it.json'); const es=require('./src/i18n/es.json'); const keys=['review.gallery','review.verdictCard','review.score','review.outOf']; keys.forEach(k => { const parts=k.split('.'); const v=parts.reduce((o,p)=>o[p], en); if(!v) { console.error('MISSING en:'+k); process.exit(1); } }); console.log('i18n keys OK');"</automated>
  </verify>
  <done>
    - ReviewJsonLd.astro outputs valid Product + Review + AggregateRating JSON-LD script tag
    - en.json, it.json, es.json all have complete review translation keys (existing + new)
    - All 3 locale files have matching key structures
  </done>
</task>

<task type="auto">
  <name>Task 3: Rebuild review page templates for all 3 locales with full component integration</name>
  <files>
    web/src/pages/en/reviews/[slug].astro
    web/src/pages/it/recensioni/[slug].astro
    web/src/pages/es/resenas/[slug].astro
  </files>
  <action>
    read_first:
    - web/src/pages/en/reviews/[slug].astro (current skeleton)
    - web/src/pages/it/recensioni/[slug].astro (current skeleton)
    - web/src/pages/es/resenas/[slug].astro (current skeleton)
    - web/src/components/common/Header.astro (to include in layout slots)
    - web/src/components/common/Footer.astro (to include in layout slots)
    - web/src/layouts/BaseLayout.astro (slot structure: header, default, footer)

    Replace all 3 skeleton review pages with the full review experience. Each page follows the same structure but with its locale ('en'/'it'/'es') and locale-specific redirect path.

    **Frontmatter** (same for all, only locale string changes):
    ```
    import { fetchBySlug } from '@lib/strapi';
    import type { StrapiReview, StrapiEntity } from '@lib/types';
    import { getStrapiMediaURL, getStrapiImageFormats, getStrapiMediaAlt } from '@lib/media';
    import { getContentStatus } from '@lib/preview';
    import { loadTranslations } from '@lib/i18n';
    import BaseLayout from '@layouts/BaseLayout.astro';
    import Header from '@components/common/Header.astro';
    import Footer from '@components/common/Footer.astro';
    import ScoreCard from '@components/review/ScoreCard.astro';
    import SpecsTable from '@components/review/SpecsTable.astro';
    import ProsConsCard from '@components/review/ProsConsCard.astro';
    import VerdictCard from '@components/review/VerdictCard.astro';
    import ReviewJsonLd from '@components/review/ReviewJsonLd.astro';

    export const prerender = false;

    const locale = 'en'; // 'it' or 'es' for other pages
    const { slug } = Astro.params;
    const status = getContentStatus(Astro.cookies);
    const t = await loadTranslations(locale);

    const review = await fetchBySlug<StrapiReview>('reviews', slug!, {
      locale,
      populate: '*',
      status,
    });

    if (!review) {
      return Astro.redirect(`/${locale}/`);
    }

    const coverImage = getStrapiMediaURL(review.gallery?.[0]);
    const coverImageAlt = getStrapiMediaAlt(review.gallery?.[0]) || review.title;
    const productImage = review.product?.images?.[0] ? getStrapiMediaURL(review.product.images[0]) : coverImage;
    const siteUrl = import.meta.env.SITE || 'https://bbqexperience.it';
    ```

    **Template structure** (page layout order, all locale pages identical except locale string and preview bar text):

    1. **Preview banner** — Keep existing preview mode banner at top (locale-specific text already exists)

    2. **Header slot** — `<Header slot="header" locale={locale} />`

    3. **Article wrapper** — `<article class="max-w-4xl mx-auto px-[var(--container-padding)] py-[var(--space-12)]">`

    4. **Hero section** — Title + excerpt + cover image + metadata:
       - `<h1 class="font-[var(--font-heading)] text-[var(--text-4xl)] uppercase tracking-tight text-[var(--color-text-primary)] mb-[var(--space-4)]" data-animate>{review.title}</h1>`
       - Metadata line: published date (formatted with locale), product brand + category if available. Use `text-[var(--color-text-muted)] text-[var(--text-sm)]`
       - Excerpt: `<p class="text-[var(--text-lg)] text-[var(--color-text-secondary)] mb-[var(--space-8)]" data-animate>{review.excerpt}</p>`
       - Cover image: full width, `rounded-[var(--radius-lg)]`, with alt text, `mb-[var(--space-12)]`

    5. **ScoreCard** — `<ScoreCard overall={review.score_overall} buildQuality={review.score_build_quality} performance={review.score_performance} value={review.score_value} easeOfUse={review.score_ease_of_use} translations={t} />`
       - Add `mb-[var(--space-12)]` wrapper div

    6. **Editorial content** — `<div class="prose prose-invert prose-lg max-w-none mb-[var(--space-12)]" set:html={review.editorial_content} data-animate />`

    7. **SpecsTable** — `<SpecsTable specifications={review.product?.specifications || null} title={getTranslation(t, 'review.specs')} />`
       - Add `mb-[var(--space-12)]` wrapper div

    8. **ProsConsCard** — `<ProsConsCard pros={review.pros} cons={review.cons} translations={t} />`
       - Add `mb-[var(--space-12)]` wrapper div

    9. **VerdictCard** — `<VerdictCard overall={review.score_overall} verdict={review.verdict} pros={review.pros} cons={review.cons} productName={review.product?.name || review.title} productImage={productImage} productImageAlt={coverImageAlt} translations={t} />`
       - Add `mb-[var(--space-12)]` wrapper div

    10. **Close article**

    11. **Footer slot** — `<Footer slot="footer" locale={locale} />`

    12. **ReviewJsonLd** — Place inside BaseLayout but outside article: `<ReviewJsonLd review={review} productImageUrl={productImage} locale={locale} siteUrl={siteUrl} />`

    13. **GSAP init script** — Add at bottom of BaseLayout content:
        ```html
        <script>
          import { initScrollAnimations } from '@lib/animations';
          document.addEventListener('DOMContentLoaded', initScrollAnimations);
        </script>
        ```

    For BaseLayout props, use `seo_title || title` for title, `seo_description || excerpt || ''` for description, pass locale and canonicalPath.

    IMPORTANT: All 3 locale pages must be structurally identical. Only these differ:
    - `locale` variable ('en', 'it', 'es')
    - Preview banner text (keep existing locale-specific text)
    - Redirect path on 404 (`/en/`, `/it/`, `/es/`)
  </action>
  <verify>
    <automated>cd C:/Users/Matteo/Desktop/Progetti/bbqexperience/web && npx astro check 2>&1 | tail -20</automated>
  </verify>
  <done>
    - EN review page at /en/reviews/[slug] renders full review with scoring, editorial, specs, pros/cons, verdict card
    - IT review page at /it/recensioni/[slug] renders same structure with Italian locale
    - ES review page at /es/resenas/[slug] renders same structure with Spanish locale
    - All pages include Header and Footer in correct slots
    - All pages include ReviewJsonLd with Schema.org structured data
    - All pages have GSAP scroll animation initialization
    - All pages support preview mode (draft content via cookie)
    - Cover image, scores, editorial, specs, pros/cons, and verdict all render correctly from Strapi data
  </done>
</task>

</tasks>

<verification>
- `cd C:/Users/Matteo/Desktop/Progetti/bbqexperience/web && npx astro check` passes with no errors
- All 5 new components exist in web/src/components/review/
- All 3 locale review pages import and render all 5 components
- ReviewJsonLd outputs a script tag with type="application/ld+json"
- i18n files have matching review key structures across all 3 locales
</verification>

<success_criteria>
- Review page displays overall score (circle) and 4 category scores (horizontal bars)
- Editorial rich text content renders from Strapi
- Product specifications render in an alternating-row table
- Pros and cons display in a 2-column layout with colored indicators
- Verdict card shows score badge, verdict text, product image, and top 2 pros/cons
- Schema.org JSON-LD contains Product, Review, and AggregateRating types
- All 3 locales (EN, IT, ES) render the same structure with localized strings
- Preview mode continues to work (draft content visible with preview cookie)
</success_criteria>

<output>
After completion, create `.planning/phases/04-review-pages/04-01-SUMMARY.md`
</output>
