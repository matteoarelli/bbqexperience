---
phase: 06-content-pages-discovery
plan: 04
type: execute
wave: 2
depends_on: ["06-01", "06-02"]
files_modified:
  - web/src/components/content/RelatedContent.astro
  - web/src/components/content/FeaturedHero.astro
  - web/src/pages/en/index.astro
  - web/src/pages/it/index.astro
  - web/src/pages/es/index.astro
  - web/src/components/content/ContentLayout.astro
  - web/src/components/common/Header.astro
autonomous: true
requirements:
  - CNT-07
  - CNT-08

must_haves:
  truths:
    - "User sees related content suggestions cross-linking reviews, recipes, and tutorials on content pages"
    - "User sees featured/seasonal content on the homepage hero curated by the author"
    - "User sees reading progress indicator and section nav on long-form content pages"
    - "User can access search from the header navigation"
  artifacts:
    - path: "web/src/components/content/RelatedContent.astro"
      provides: "Related content cross-linking component"
      min_lines: 30
    - path: "web/src/components/content/FeaturedHero.astro"
      provides: "Homepage hero with featured content from CMS"
      min_lines: 40
  key_links:
    - from: "web/src/components/content/RelatedContent.astro"
      to: "/api/tutorials, /api/reviews, /api/recipes"
      via: "fetchCollection from @lib/strapi"
      pattern: "fetchCollection"
    - from: "web/src/components/content/FeaturedHero.astro"
      to: "/api/blog-posts"
      via: "fetchCollection with featured filter"
      pattern: "featured.*true"
    - from: "web/src/components/content/ContentLayout.astro"
      to: "ReadingProgress.svelte"
      via: "Svelte island with client:idle"
      pattern: "client:idle"
---

<objective>
Wire up the remaining discovery features: related content cross-linking on article pages, featured/seasonal content on the homepage hero, reading progress indicator on content pages, and search access from the header.

Purpose: These features complete the content discovery experience, connecting isolated content pages into a navigable network and adding the reading aids that make long-form content enjoyable.
Output: Fully connected content discovery system with cross-linking, featured hero, reading progress, and search.
</objective>

<execution_context>
@C:/Users/Matteo/Desktop/Progetti/bbqexperience/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Matteo/Desktop/Progetti/bbqexperience/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-content-pages-discovery/06-01-SUMMARY.md
@.planning/phases/06-content-pages-discovery/06-02-SUMMARY.md

<interfaces>
<!-- Svelte components from Plan 02 -->
```
web/src/components/content/ReadingProgress.svelte — Props: locale: string. Uses client:idle.
web/src/components/content/SearchDialog.svelte — Props: locale: string, translations: Record<string, string>. Uses client:idle.
```

<!-- Shared components from Plan 01 -->
```
web/src/components/content/ContentLayout.astro — Has slot for "related" content below article
web/src/components/content/ArticleCard.astro — Used for related content cards
```

<!-- Strapi API -->
```typescript
fetchCollection<StrapiTutorial>('tutorials', { locale, pageSize: 3, sort: 'published_date:desc' })
fetchCollection<StrapiReview>('reviews', { locale, pageSize: 3, sort: 'published_date:desc' })
fetchCollection<StrapiRecipe>('recipes', { locale, pageSize: 3, sort: 'published_date:desc' })
fetchCollection<StrapiBlogPost>('blog-posts', { locale, filters: { featured: { $eq: true } } })
```

<!-- i18n routes -->
```typescript
localizedRoutes.tutorials[locale]  // en: 'tutorials', it: 'guide', es: 'tutoriales'
localizedRoutes.reviews[locale]    // en: 'reviews', it: 'recensioni', es: 'resenas'
localizedRoutes.recipes[locale]    // en: 'recipes', it: 'ricette', es: 'recetas'
localizedRoutes.blog[locale]       // en: 'blog', it: 'blog', es: 'blog'
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create RelatedContent and FeaturedHero components</name>
  <files>
    web/src/components/content/RelatedContent.astro
    web/src/components/content/FeaturedHero.astro
  </files>
  <read_first>
    web/src/components/content/ArticleCard.astro
    web/src/components/content/ContentLayout.astro
    web/src/components/common/Hero.astro
    web/src/lib/strapi.ts
    web/src/lib/types.ts
    web/src/lib/i18n.ts
    web/src/i18n/en.json
    web/src/pages/en/index.astro
  </read_first>
  <action>
    **RelatedContent.astro** — Props: `locale: Locale`, `translations: Record<string, any>`, `currentSlug: string`, `currentType: 'tutorial' | 'blog-post'`. Fetches related content from multiple content types and renders a "Related Content" section:
    - Fetch 3 recent tutorials (exclude current if type matches), 3 recent reviews, 3 recent recipes. Use `fetchCollection` with `pageSize: 3`, `sort: 'published_date:desc'`, `populate: 'cover_image'`.
    - Filter out the current article by slug to avoid self-reference.
    - Combine into a single array, shuffle, take first 4-6 items.
    - Render as a section with heading "Related Content" (from translations: `content.relatedContent`).
    - Use a horizontal scrollable row on mobile (flex, overflow-x-auto, snap) or 3-column grid on desktop.
    - Each item uses ArticleCard with appropriate href based on content type. Add a small content-type label (Review, Recipe, Tutorial) as a badge on each card.
    - Wrap in a `<section>` with bottom margin and a subtle top border separator.

    **FeaturedHero.astro** — Props: `locale: Locale`, `translations: Record<string, any>`. Replaces or augments the homepage hero with featured content:
    - Fetch featured blog posts: `fetchCollection<StrapiBlogPost>('blog-posts', { locale, filters: { featured: { $eq: true } }, populate: 'cover_image', sort: 'published_date:desc', pageSize: 3 })`.
    - If no featured posts exist, fall back to the existing static hero (render the current Hero component).
    - If featured posts exist, render a hero section with:
      - Large featured article card (first result): full-width background cover image, title overlay with gradient, excerpt, "Read more" CTA linking to the blog post.
      - Below: 2 smaller featured cards in a row (if available).
    - Section heading: "Featured Content" or use `content.featuredContent` from translations.
    - Maintain the dark theme and fire accent aesthetic. Cover image uses gradient overlay (transparent to dark) for text readability.
    - Keep the main hero title "BBQ EXPERIENCE" above the featured section, but make it more compact (less vertical space) when featured content is present.
  </action>
  <verify>
    <automated>cd C:/Users/Matteo/Desktop/Progetti/bbqexperience/web && npx astro check 2>&1 | tail -20</automated>
  </verify>
  <done>
    - RelatedContent.astro fetches cross-type content and renders 4-6 related items
    - FeaturedHero.astro shows featured blog posts from CMS when available
    - FeaturedHero falls back to static hero when no featured content exists
    - Both components follow dark theme design tokens
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire ReadingProgress, SearchDialog, RelatedContent, and FeaturedHero into pages</name>
  <files>
    web/src/components/content/ContentLayout.astro
    web/src/components/common/Header.astro
    web/src/pages/en/index.astro
    web/src/pages/it/index.astro
    web/src/pages/es/index.astro
    web/src/pages/en/tutorials/[slug].astro
    web/src/pages/it/guide/[slug].astro
    web/src/pages/es/tutoriales/[slug].astro
    web/src/pages/en/blog/[slug].astro
    web/src/pages/it/blog/[slug].astro
    web/src/pages/es/blog/[slug].astro
  </files>
  <read_first>
    web/src/components/content/ContentLayout.astro
    web/src/components/common/Header.astro
    web/src/components/content/ReadingProgress.svelte
    web/src/components/content/SearchDialog.svelte
    web/src/components/content/RelatedContent.astro
    web/src/components/content/FeaturedHero.astro
    web/src/pages/en/index.astro
    web/src/pages/en/tutorials/[slug].astro
    web/src/pages/en/blog/[slug].astro
    web/src/i18n/en.json
  </read_first>
  <action>
    **Wire ReadingProgress into ContentLayout.astro:**
    - Import ReadingProgress from `@components/content/ReadingProgress.svelte`.
    - Add `<ReadingProgress locale={locale} client:idle />` at the top of the layout, after the cover image but positioned fixed (the component handles its own positioning).
    - The component will automatically find h2[id] elements in the content area.

    **Wire SearchDialog into Header.astro:**
    - Import SearchDialog from `@components/content/SearchDialog.svelte`.
    - Add the SearchDialog component in the header's right section, before the LanguageSwitcher.
    - Pass `locale={locale}` and a `translations` object with the search-related keys (searchPlaceholder, noArticles from the translations prop).
    - Use `client:idle` directive.
    - On mobile, show only the magnifying glass icon (SearchDialog handles the trigger button rendering).

    **Wire RelatedContent into detail pages:**
    - In all tutorial [slug].astro pages: import RelatedContent, render it below the ContentLayout (in the "related" slot or after ContentLayout). Pass `currentSlug={slug}`, `currentType="tutorial"`, `locale`, `translations`.
    - In all blog [slug].astro pages: same but `currentType="blog-post"`.

    **Wire FeaturedHero into homepage:**
    - In all 3 homepage index.astro files (en, it, es):
      - Import FeaturedHero.
      - Replace the current static Hero component with FeaturedHero.
      - Remove the placeholder "Content sections will be added in future phases" section.
      - Keep the Hero component import as FeaturedHero uses it internally as fallback.
  </action>
  <verify>
    <automated>cd C:/Users/Matteo/Desktop/Progetti/bbqexperience/web && npx astro check 2>&1 | tail -20</automated>
  </verify>
  <done>
    - Reading progress bar appears on tutorial and blog detail pages when scrolling
    - Search button visible in Header, opens search modal
    - Related content section shows cross-type suggestions below articles
    - Homepage shows featured content from CMS (or falls back to static hero)
    - All wiring works across all 3 locales
  </done>
</task>

</tasks>

<verification>
- `npx astro check` passes
- `npx astro build` completes
- Homepage renders FeaturedHero with CMS content (or static fallback)
- Tutorial detail pages show ReadingProgress bar, RelatedContent section, and ArticleSchema JSON-LD
- Blog detail pages show ReadingProgress bar, RelatedContent section, and ArticleSchema JSON-LD
- Header shows search trigger button in all pages
- All 3 locales function correctly
</verification>

<success_criteria>
- User sees related content (reviews, recipes, tutorials) at the bottom of every article page
- User sees featured/seasonal content on the homepage hero (curated via CMS `featured` boolean)
- User sees reading progress indicator with section navigation when scrolling articles
- User can access search from the header on any page
- Homepage no longer shows placeholder "Content sections will be added in future phases"
</success_criteria>

<output>
After completion, create `.planning/phases/06-content-pages-discovery/06-04-SUMMARY.md`
</output>
