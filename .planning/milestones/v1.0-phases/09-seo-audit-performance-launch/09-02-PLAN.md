---
phase: 09-seo-audit-performance-launch
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/pages/404.astro
  - web/src/i18n/en.json
  - web/src/i18n/it.json
  - web/src/i18n/es.json
autonomous: true
requirements: [DES-04, DES-07]

must_haves:
  truths:
    - "Visiting a non-existent URL shows a branded 404 page with BBQ Experience styling"
    - "The 404 page offers search functionality and navigation links to help users find content"
    - "The 404 page works for all three locales with translated text"
  artifacts:
    - path: "web/src/pages/404.astro"
      provides: "Branded 404 error page"
      min_lines: 40
      contains: "404"
  key_links:
    - from: "web/src/pages/404.astro"
      to: "web/src/layouts/BaseLayout.astro"
      via: "import BaseLayout"
      pattern: "BaseLayout"
    - from: "web/src/pages/404.astro"
      to: "web/src/components/common/Header.astro"
      via: "import Header"
      pattern: "Header"
---

<objective>
Create a branded 404 error page and perform a Lighthouse performance audit pass to ensure launch readiness.

Purpose: Complete the branded error experience (DES-07) and verify/fix performance issues for Lighthouse 90+ (DES-04).
Output: 404.astro page, i18n keys for error page, performance fixes identified and applied.
</objective>

<execution_context>
@C:/Users/Matteo/Desktop/Progetti/bbqexperience/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Matteo/Desktop/Progetti/bbqexperience/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
From web/src/layouts/BaseLayout.astro:
```typescript
interface Props {
  title: string;
  description: string;
  locale?: string;
  canonicalPath?: string;
  ogImage?: string;
  class?: string;
}
```

From web/src/components/common/Header.astro (used in all pages):
- Requires: locale, translations, currentPath props

From web/src/components/common/Footer.astro (used in all pages):
- Requires: locale, translations props

Existing translation keys in en.json:
- common.notFound: "Page not found"
- common.error: "Something went wrong"
- nav.home: "Home"
- nav.reviews: "Reviews"
- nav.recipes: "Recipes"
- nav.tutorials: "Tutorials"
- nav.blog: "Blog"
- content.searchPlaceholder: "Search articles..."

SearchDialog.svelte exists at web/src/components/content/SearchDialog.svelte (Pagefind-based).

Design tokens from tokens.css:
- --color-accent-fire: #F97316 (dark), #EA580C (light)
- --color-bg-primary: #0D0D0D (dark), #FAFAF9 (light)
- --font-heading: 'Oswald Variable'
- --text-5xl for large display text
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create branded 404 page with search and navigation</name>
  <files>web/src/pages/404.astro, web/src/i18n/en.json, web/src/i18n/it.json, web/src/i18n/es.json</files>
  <action>
    read_first:
    - web/src/pages/en/index.astro (page structure pattern)
    - web/src/layouts/BaseLayout.astro (layout props)
    - web/src/i18n/en.json (existing keys)
    - web/src/i18n/it.json (existing keys)
    - web/src/i18n/es.json (existing keys)
    - web/src/components/content/SearchDialog.svelte (search component interface)
    - web/src/components/common/Header.astro (header props)

    1. Add translation keys to all 3 locale JSON files under `"error"` namespace:
       - en.json: `"error": { "title": "Page Not Found", "heading": "404", "message": "The page you're looking for doesn't exist or has been moved.", "searchPrompt": "Try searching for what you need:", "browsePrompt": "Or browse our content:", "backHome": "Back to Homepage" }`
       - it.json: `"error": { "title": "Pagina Non Trovata", "heading": "404", "message": "La pagina che stai cercando non esiste o e stata spostata.", "searchPrompt": "Prova a cercare quello che ti serve:", "browsePrompt": "Oppure esplora i nostri contenuti:", "backHome": "Torna alla Homepage" }`
       - es.json: `"error": { "title": "Pagina No Encontrada", "heading": "404", "message": "La pagina que buscas no existe o ha sido movida.", "searchPrompt": "Intenta buscar lo que necesitas:", "browsePrompt": "O explora nuestro contenido:", "backHome": "Volver a la Pagina Principal" }`

    2. Create `web/src/pages/404.astro`:
       - Detect locale from `Astro.url.pathname` — extract first path segment if it matches en/it/es, default to 'en'.
       - Import and use BaseLayout, Header, Footer with the detected locale.
       - Load translations via `loadTranslations(locale)`.
       - Design: centered layout with these sections top-to-bottom:
         a. Large "404" heading in --font-heading at --text-5xl, color --color-accent-fire
         b. "Page Not Found" subtitle in --text-2xl, color --color-text-primary
         c. Description message in --text-base, color --color-text-muted, max-width 480px centered
         d. Search section: heading "Try searching..." in --text-lg, then an inline search form. Use a simple `<form>` that redirects to `/{locale}/?q={query}` OR just link to search. Since Pagefind uses a dialog, add a button styled like the CTA (bg --color-accent-fire, white text, rounded) that says "Search" and triggers Pagefind search dialog. Import SearchDialog.svelte with `client:load` and wire the button to open it.
         e. Navigation links section: heading "Or browse our content:", then a grid of 4 cards linking to Reviews, Recipes, Tutorials, Blog — each card with the section name, styled with --color-bg-secondary background, --color-border-default border, hover --color-accent-fire border, rounded-lg, padding var(--space-4).
         f. "Back to Homepage" link styled as secondary button (border --color-accent-fire, text --color-accent-fire, hover bg --color-accent-fire text white).
       - All text from translation keys (error.heading, error.title, error.message, etc.).
       - Use nav.reviews, nav.recipes, nav.tutorials, nav.blog for card labels.
       - Links: `/{locale}/reviews/`, `/{locale}/recipes/`, `/{locale}/tutorials/`, `/{locale}/blog/` (use localizedRoutes for it/es).
       - Use scoped `<style>` with CSS custom properties from tokens.css.
       - Add `data-animate` to heading and cards for scroll animation consistency.

    acceptance_criteria:
    - `grep -c "404" web/src/pages/404.astro` returns >= 2
    - `grep -c "error.title" web/src/pages/404.astro` returns >= 1
    - `grep -c '"error"' web/src/i18n/en.json` returns >= 1
    - `grep -c '"error"' web/src/i18n/it.json` returns >= 1
    - `grep -c '"error"' web/src/i18n/es.json` returns >= 1
    - `grep -c "SearchDialog" web/src/pages/404.astro` returns >= 1
    - `grep -c "BaseLayout" web/src/pages/404.astro` returns >= 1
  </action>
  <verify>
    <automated>cd C:/Users/Matteo/Desktop/Progetti/bbqexperience/web && test -f src/pages/404.astro && grep -c "404" src/pages/404.astro && grep -c "BaseLayout" src/pages/404.astro && grep -c "SearchDialog" src/pages/404.astro && grep -c '"error"' src/i18n/en.json && grep -c '"error"' src/i18n/it.json && grep -c '"error"' src/i18n/es.json</automated>
  </verify>
  <done>Branded 404 page exists at web/src/pages/404.astro with BBQ Experience styling, search functionality via Pagefind SearchDialog, navigation links to all content sections, and i18n support for en/it/es.</done>
</task>

<task type="auto">
  <name>Task 2: Lighthouse performance audit and fixes</name>
  <files>web/src/layouts/BaseLayout.astro, web/src/components/common/SEOHead.astro</files>
  <action>
    read_first:
    - web/src/layouts/BaseLayout.astro (current head structure)
    - web/src/components/common/SEOHead.astro (current meta tags)
    - web/src/styles/global.css (font loading)
    - web/astro.config.mjs (build config)

    Audit and fix common Lighthouse issues. The site is Astro SSG with near-zero JS by default, so Performance should already be high. Focus on:

    1. **SEOHead.astro fixes (per DES-04 — SEO + Best Practices):**
       - Ensure `<meta name="robots" content="index, follow">` is present
       - Add `<meta property="og:locale:alternate">` tags for other locales (in addition to existing og:locale)
       - Verify og:type defaults correctly (already 'website', good)

    2. **BaseLayout.astro fixes (per DES-04 — Performance + Accessibility):**
       - Add `<link rel="preconnect" href="https://fonts.googleapis.com">` if Google Fonts are used (check global.css for @import or link to fonts)
       - If fonts are loaded via @font-face with local files, add `<link rel="preload" as="font" type="font/woff2" crossorigin>` for the primary fonts (Oswald, Inter) — only if the font files are in public/fonts/
       - Add `<meta name="color-scheme" content="dark light">` for browser UI theming
       - Verify the existing skip-to-content link is correctly implemented (it is)
       - Add `<link rel="icon" type="image/svg+xml" href="/favicon.svg">` if not present (check if any favicon exists in public/)
       - Add `<link rel="apple-touch-icon" href="/apple-touch-icon.png">` if file exists

    3. **Accessibility quick wins:**
       - Check that html lang attribute is set (already is: `lang={locale}`)
       - Verify viewport meta is correct (already is)

    NOTE: Do NOT over-engineer this. Astro sites score 90+ out of the box. Only add what's actually missing. Read the files first, then decide what's needed.

    acceptance_criteria:
    - `grep -c "robots" web/src/components/common/SEOHead.astro` returns >= 1
    - `grep -c "color-scheme" web/src/layouts/BaseLayout.astro` returns >= 1
  </action>
  <verify>
    <automated>cd C:/Users/Matteo/Desktop/Progetti/bbqexperience/web && grep -c "robots" src/components/common/SEOHead.astro && grep -c "color-scheme" src/layouts/BaseLayout.astro</automated>
  </verify>
  <done>SEOHead.astro includes robots meta tag and og:locale:alternate tags. BaseLayout.astro includes color-scheme meta. All standard Lighthouse audit items addressed without over-engineering.</done>
</task>

</tasks>

<verification>
- `test -f web/src/pages/404.astro` confirms 404 page exists
- `grep -r "error\." web/src/pages/404.astro` shows translation key usage
- `grep "robots" web/src/components/common/SEOHead.astro` confirms robots meta
- `grep "color-scheme" web/src/layouts/BaseLayout.astro` confirms color-scheme meta
</verification>

<success_criteria>
- Branded 404 page renders with BBQ Experience dark theme, large 404 heading, search button, and navigation cards
- 404 page detects locale from URL path and renders in correct language
- All 3 locale JSON files have error namespace translations
- SEOHead.astro includes robots meta tag for indexing
- BaseLayout.astro includes color-scheme meta for browser theming
</success_criteria>

<output>
After completion, create `.planning/phases/09-seo-audit-performance-launch/09-02-SUMMARY.md`
</output>
