---
phase: 10-debt-closure-measurement-baseline
plan: 05
type: execute
wave: 3
depends_on: [10-04]
conditional: true
conditional_trigger: ".planning/artifacts/lighthouse-v1.1-baseline/SUMMARY.md frontmatter status == gaps_found"
files_modified:
  - web/src/
  - .planning/artifacts/lighthouse-v1.1-baseline/
autonomous: false
requirements:
  - DEBT-03
tags: [lighthouse, performance, fixes, conditional]
must_haves:
  truths:
    - "Every page flagged sub-90 in Plan 04 SUMMARY has a shipped fix on production"
    - "Re-measurement of fixed pages produces JSON report with all 4 categories ≥90"
    - "Updated SUMMARY.md records before/after scores per fixed page"
    - "Phase 10 success criterion 4 satisfied: 'Any page scoring below 90 has a fix shipped to production and a re-measured report included in the baseline folder'"
  artifacts:
    - path: ".planning/artifacts/lighthouse-v1.1-baseline/{id}-after.json"
      provides: "Re-measurement of each previously sub-90 page post-fix"
      contains: "categories.{performance,accessibility,best-practices,seo}.score ≥0.90"
    - path: ".planning/artifacts/lighthouse-v1.1-baseline/SUMMARY.md"
      provides: "Updated summary with before/after table and DEBT-03 closure attestation"
      contains: "Before/After section, status: pass"
    - path: "web/src/ (specific files TBD from Plan 04 findings)"
      provides: "Fix implementations targeting the specific Lighthouse audit failures"
      contains: "Commit references in git log"
  key_links:
    - from: "Fix commits"
      to: "production deploy via adnanh/webhook"
      via: "git push → /opt/webhooks/scripts/deploy-bbqexperience.sh"
      pattern: "bbqexperience.log.*success|docker.*build.*success"
    - from: "Re-measurement JSON"
      to: "post-deploy production URL"
      via: "Lighthouse CLI run after deploy confirmed"
      pattern: "categories.performance.score.*0\\.(9|[1-9][0-9])"
---

<objective>
**CONDITIONAL PLAN — execute only if Plan 04 SUMMARY.md frontmatter reports `status: gaps_found`.**

For every page that scored <90 in any category during the Plan 04 baseline measurement: diagnose the Lighthouse audit failures, ship a fix to production via the adnanh/webhook deploy pipeline, wait for the rebuild to complete, re-run Lighthouse against the fixed page, and confirm all 4 categories now score ≥90.

Purpose: Close DEBT-03 fully by satisfying phase success criterion 4: "Any page scoring below 90 has a fix shipped to production and a re-measured report included in the baseline folder".

Output:
- Production code changes (exact files depend on Plan 04 findings — cannot be pre-specified)
- One `{id}-after.json` Lighthouse report per fixed page
- Updated `SUMMARY.md` with before/after score table and final DEBT-03 disposition

Autonomy: `autonomous: false`. This plan requires judgment on each sub-90 finding (which audit to prioritize, which fix strategy), so it pauses for human checkpoint after diagnosis and before deploy.
</objective>

<execution_context>
@C:/Users/Matteo/Desktop/Progetti/bbqexperience/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Matteo/Desktop/Progetti/bbqexperience/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/artifacts/lighthouse-v1.1-baseline/SUMMARY.md
@.planning/artifacts/lighthouse-v1.1-baseline/selected-slugs.json
@.planning/STATE.md
@CLAUDE.md
</context>

<interfaces>
<!-- Deploy pipeline: git push → adnanh/webhook → rebuild-web.sh → Docker build -->

Deploy verification after push:
```bash
ssh -i "C:/Users/Matteo/Desktop/Progetti/Hetzner Server/hetzner_production" \
    -o StrictHostKeyChecking=no root@204.168.153.43 \
    'tail -30 /opt/webhooks/logs/bbqexperience.log'
```

Expected log tail after successful deploy: lines including "Build succeeded", "docker compose up -d --build web", "Smoke test passed".

Common Lighthouse audit → fix mappings (reference only; actual fix depends on Plan 04 findings):
- `largest-contentful-paint` (LCP) slow → check hero image has `fetchpriority="high"`, confirm OptimizedImage generates AVIF
- `unused-javascript` large → check for full-framework imports in Astro islands; use per-island dynamic imports
- `unused-css-rules` large → Tailwind should purge already; verify no global CSS imports from unused components
- `color-contrast` failures → adjust design token contrast in `tokens.css`; verify against WCAG AA
- `meta-description` missing → add default via `SEOHead.astro` for pages without CMS-provided description
- `image-alt` missing → audit `<img>` / `<Image>` usage across component files
- `heading-order` → fix incorrect `<h3>` before `<h2>` in page templates
- `render-blocking-resources` → ensure all CSS is critical-inlined or preloaded; no blocking `<script>` without `async`/`defer`
- `uses-responsive-images` → confirm OptimizedImage wraps everything user-facing; avoid raw `<img>` for CMS media
</interfaces>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Checkpoint — confirm Plan 04 findings and decide fix approach</name>
  <decision>Which sub-90 audits will this plan fix, and in what order?</decision>
  <context>
Plan 04 SUMMARY.md reported `status: gaps_found`. Before writing any code, Matteo (user) confirms:
1. The specific page IDs and categories flagged sub-90
2. The root-cause Lighthouse audits per page (from the JSON reports' `.audits` section where `score < 0.9`)
3. The fix priority order (usually: shared/systemic issues first, per-page issues second)
4. Scope boundary: pure Lighthouse fixes, no feature creep
  </context>
  <read_first>
    - .planning/artifacts/lighthouse-v1.1-baseline/SUMMARY.md (Plan 04 findings)
    - .planning/artifacts/lighthouse-v1.1-baseline/ (each sub-90 JSON — use jq to extract failing audits: `jq '.audits | to_entries | map(select(.value.score != null and .value.score < 0.9)) | map({id: .key, title: .value.title, score: .value.score})' <file>`)
  </read_first>
  <action>
Before proceeding to auto tasks:

1. Read Plan 04's SUMMARY.md "Sub-90 Findings" section and tabulate in this plan's scratch:
```
Page ID | Category | Score | Top failing audit | Suggested fix
```

2. For each sub-90 JSON report, extract the specific failing audits:
```bash
for f in .planning/artifacts/lighthouse-v1.1-baseline/*.json; do
  [[ "$f" == *-after.json ]] && continue
  [[ "$f" == *SUMMARY* ]] && continue
  echo "=== $f ==="
  jq '.audits | to_entries | map(select(.value.score != null and .value.score < 0.9 and .value.scoreDisplayMode != "notApplicable")) | map({id: .key, title: .value.title, score: .value.score, displayValue: .value.displayValue}) | sort_by(.score)' "$f"
done
```

3. Draft a proposed fix plan covering every failing audit. Group by shared vs page-specific:
   - Shared fixes (touch a layout/component used by many pages) — apply once, re-measure all affected pages
   - Per-page fixes (slug-specific content issues) — apply individually

4. Present the fix list to Matteo. Wait for `approved` or revisions.
  </action>
  <what-built>Diagnostic summary of all sub-90 audit failures grouped by root cause + proposed fix plan with priority order.</what-built>
  <how-to-verify>
    1. Review the diagnostic table listing every sub-90 page + specific failing audit
    2. Confirm the fix approach addresses root causes (not just symptoms)
    3. Confirm scope is limited to Lighthouse fixes (no feature creep)
    4. Reply "approved" to proceed with fix implementation, or describe revisions needed
  </how-to-verify>
  <resume-signal>Type "approved" or describe revisions</resume-signal>
  <verify>
    <automated>MISSING — checkpoint tasks are human-gated by design; no automated command.</automated>
  </verify>
  <acceptance_criteria>
    - A diagnostic table mapping every sub-90 finding from Plan 04 to a specific failing audit (with audit ID from Lighthouse) exists in this task's scratch
    - Every fix in the proposed plan references a specific file to modify (or explicit justification for not modifying code, e.g., "CMS-side content update")
    - User has replied "approved" (or equivalent) before moving to Task 2
  </acceptance_criteria>
  <done>Fix plan approved by Matteo, scope locked.</done>
</task>

<task type="auto">
  <name>Task 2: Implement approved fixes and push to production</name>
  <files>web/src/ (specific files determined by Task 1 approved plan)</files>
  <read_first>
    - The files identified in Task 1's approved fix plan (MUST read each before editing)
    - CLAUDE.md conventions (Italian comments for code, convention for shared components)
    - .planning/artifacts/lighthouse-v1.1-baseline/SUMMARY.md
  </read_first>
  <action>
Implement each approved fix:

1. For each fix in the Task 1 plan, use the Edit tool on the specific file(s). Respect CLAUDE.md conventions:
   - Code comments in Italian
   - i18n via custom JSON (not Paraglide)
   - No new lenis/locomotive dependencies
   - Don't hand-roll patterns that already exist in the codebase

2. Run local build to catch breakage before push:
```bash
cd web && npm run build
```
If the build fails, fix the breakage (don't push broken code). Iterate until `npm run build` exits 0.

3. Commit in small atomic commits per fix:
```bash
git add <specific files>
git commit -m "perf(10-05): <fix description> for <page(s)>"
```

4. Push to main (triggers adnanh/webhook auto-deploy):
```bash
git push origin main
```

5. Wait for the webhook deploy to complete. Poll the deploy log via SSH:
```bash
ssh -i "C:/Users/Matteo/Desktop/Progetti/Hetzner Server/hetzner_production" \
    -o StrictHostKeyChecking=no root@204.168.153.43 \
    'tail -40 /opt/webhooks/logs/bbqexperience.log'
```
Expected: "Build succeeded", "docker compose up -d --build web", container healthy. Poll every 30 seconds for up to 10 minutes.

6. Confirm the fix is actually live via curl probe (content or header check specific to the fix — if the fix added a meta tag, grep for it on the live URL).

IMPORTANT: Do NOT skip the SSH deploy verification. CLAUDE.md says "NON usare GitHub Actions per il deploy — i workflow deploy.yml sono stati disabilitati. Il deploy avviene tramite webhook self-hosted (adnanh/webhook)". The webhook fires on push; you must confirm it succeeded.
  </action>
  <verify>
    <automated>bash -c "cd web && npm run build > /dev/null 2>&1 && echo 'Build passes' && cd .. && git log --oneline | head -5 | grep -qE 'perf\\(10-05\\)|fix\\(10-05\\)'"</automated>
  </verify>
  <acceptance_criteria>
    - All approved fixes from Task 1 are implemented
    - Local `npm run build` exits 0 after all changes
    - Each fix committed with message format `perf(10-05): ...` or `fix(10-05): ...`
    - `git push origin main` completed without error
    - Deploy log on Hetzner shows "Build succeeded" and container restart for the web container
    - Curl probe of at least one fixed page confirms the change is live (fix-specific — e.g., if fix was adding alt attributes, the live HTML contains them)
    - Code comments in Italian per CLAUDE.md convention
  </acceptance_criteria>
  <done>All fixes live on production, build passes, deploy verified via SSH log.</done>
</task>

<task type="auto">
  <name>Task 3: Re-measure fixed pages and update SUMMARY.md with before/after scores</name>
  <files>
    .planning/artifacts/lighthouse-v1.1-baseline/SUMMARY.md
  </files>
  <read_first>
    - .planning/artifacts/lighthouse-v1.1-baseline/SUMMARY.md (current state with sub-90 findings)
    - .planning/artifacts/lighthouse-v1.1-baseline/selected-slugs.json
    - Each {id}.json file for pages that were sub-90 (before scores)
  </read_first>
  <action>
For every page ID that was flagged sub-90 in Plan 04's SUMMARY.md (and, if a shared fix touched additional pages not originally sub-90, re-measure those too to confirm no regression):

1. Run Lighthouse again with the same parameters as Plan 04:
```bash
cd .planning/artifacts/lighthouse-v1.1-baseline
npx --yes lighthouse "<url-from-selected-slugs.json>" \
  --only-categories=performance,accessibility,best-practices,seo \
  --form-factor=mobile \
  --throttling-method=simulate \
  --output=json \
  --output-path="./${id}-after.json" \
  --chrome-flags="--headless=new --no-sandbox --disable-gpu" \
  --quiet
```

2. Validate the new report has all 4 category scores ≥0.90:
```bash
jq -e '.categories.performance.score >= 0.90 and .categories.accessibility.score >= 0.90 and .categories["best-practices"].score >= 0.90 and .categories.seo.score >= 0.90' "./${id}-after.json"
```

3. If any category is still <90, return to Task 1 checkpoint with the new failing audits as input (iterate).

4. Once all re-measured pages pass ≥90 in all 4 categories, update `SUMMARY.md`:
   - Change frontmatter `status: gaps_found` → `status: pass`
   - Change frontmatter `pages_below_90: N` → `pages_below_90: 0`
   - Add a new section:

```markdown
## Before / After (Plan 05 Fixes)

| Page | Category | Before | After | Fix | Commit |
|------|----------|--------|-------|-----|--------|
| {id} | {category} | {pre} | {post} | {fix summary} | {commit SHA} |

All previously sub-90 pages now score ≥90 in all 4 categories.

## DEBT-03 Final Status

**CLOSED** — measurement + fixes both complete. v1.1 feature work starts on a firm performance baseline.
```

5. Update the final "Reproducibility" section noting the after-reports:
```
After-fix reports: *-after.json (one per remediated page)
```

The SUMMARY.md now contains: frontmatter status=pass, before table (from Plan 04), before/after fix table (new), DEBT-03 closed attestation.
  </action>
  <verify>
    <automated>bash -c "cd .planning/artifacts/lighthouse-v1.1-baseline && grep -q '^status: pass$' SUMMARY.md && grep -q 'Before / After' SUMMARY.md && grep -q 'DEBT-03 Final Status' SUMMARY.md && grep -q 'CLOSED' SUMMARY.md && for f in *-after.json; do [ -f \"\$f\" ] && jq -e '.categories.performance.score >= 0.90 and .categories.accessibility.score >= 0.90 and .categories[\"best-practices\"].score >= 0.90 and .categories.seo.score >= 0.90' \"\$f\" > /dev/null || { echo \"FAIL: \$f scores not all >= 0.90\"; exit 1; }; done; echo OK"</automated>
  </verify>
  <acceptance_criteria>
    - One `{id}-after.json` file exists per originally sub-90 page
    - Every `*-after.json` has `categories.{performance,accessibility,best-practices,seo}.score >= 0.90`
    - `SUMMARY.md` frontmatter `status: pass`
    - `SUMMARY.md` contains "Before / After" section with per-page before/after scores + commit SHA
    - `SUMMARY.md` contains "DEBT-03 Final Status: CLOSED" line
    - Phase 10 success criterion 4 ("Any page scoring below 90 has a fix shipped and a re-measured report included") is verifiably satisfied
  </acceptance_criteria>
  <done>All fixed pages score ≥90, SUMMARY.md reflects closure, DEBT-03 fully satisfied.</done>
</task>

</tasks>

<verification>
After all tasks complete:
- `.planning/artifacts/lighthouse-v1.1-baseline/SUMMARY.md` frontmatter has `status: pass`
- At least one `*-after.json` file exists per originally sub-90 page
- Each after-file has all 4 category scores ≥0.90
- Production deploy succeeded (verified via SSH log + live URL probe)
- Phase 10 fully complete
</verification>

<success_criteria>
- All originally sub-90 pages now score ≥90 in Performance, Accessibility, Best Practices, and SEO on mobile Lighthouse
- Before/After scores documented in SUMMARY.md
- Fix commits live on production, verified via SSH deploy log
- DEBT-03 fully CLOSED
- Phase 10 Goal achieved: "v1.0 acceptance baseline is fully documented and measured so v1.1 feature work starts on firm ground"
</success_criteria>

<output>
Create `.planning/phases/10-debt-closure-measurement-baseline/10-05-SUMMARY.md` with:
- `requirements_completed: [DEBT-03]`
- Commit SHAs of the fix commits
- Before/after score summary table
- Deploy verification log snippet

Commit:
`node .claude/get-shit-done/bin/gsd-tools.cjs commit "docs(10): close DEBT-03 — ship fixes + re-measure to 90+" --files .planning/artifacts/lighthouse-v1.1-baseline/ .planning/phases/10-debt-closure-measurement-baseline/10-05-SUMMARY.md`
</output>
