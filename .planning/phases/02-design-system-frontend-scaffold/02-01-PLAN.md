---
phase: 02-design-system-frontend-scaffold
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/astro.config.mjs
  - web/package.json
  - web/src/styles/global.css
  - web/src/styles/tokens.css
  - web/src/layouts/BaseLayout.astro
  - web/src/components/common/OptimizedImage.astro
  - web/src/components/common/SEOHead.astro
  - web/public/fonts/README.txt
  - web/tsconfig.json
autonomous: true
requirements: [DES-01, DES-03, DES-05, SEO-02]

must_haves:
  truths:
    - "Site renders with dark background (#0D0D0D) and fire/amber accent colors"
    - "Pages include proper hreflang tags for en, it, es locales"
    - "Pages include canonical URL tags"
    - "Layout is mobile-first with responsive breakpoints"
    - "Images use WebP/AVIF format with lazy loading and srcset"
  artifacts:
    - path: "web/src/styles/global.css"
      provides: "Tailwind 4 import + global dark theme styles"
      contains: "@import"
    - path: "web/src/styles/tokens.css"
      provides: "CSS custom properties for BBQ design tokens"
      contains: "--color-bg-primary"
    - path: "web/src/layouts/BaseLayout.astro"
      provides: "Base HTML layout with hreflang, canonical, responsive meta"
      contains: "hreflang"
      min_lines: 40
    - path: "web/src/components/common/OptimizedImage.astro"
      provides: "Image component with WebP/AVIF, lazy loading, srcset"
      contains: "loading"
    - path: "web/src/components/common/SEOHead.astro"
      provides: "SEO head component with hreflang and canonical tags"
      contains: "canonical"
  key_links:
    - from: "web/src/layouts/BaseLayout.astro"
      to: "web/src/styles/global.css"
      via: "CSS import"
      pattern: "global.css"
    - from: "web/src/layouts/BaseLayout.astro"
      to: "web/src/components/common/SEOHead.astro"
      via: "component import"
      pattern: "SEOHead"
---

<objective>
Create the BBQ Experience design system foundation: Tailwind 4 integration with dark-theme CSS custom properties, the BaseLayout with SEO meta tags (hreflang, canonical), and an optimized image component.

Purpose: Establish the visual identity and SEO foundation that every page in the site will inherit.
Output: Design tokens, base layout, SEO head component, optimized image component, Tailwind 4 configured.
</objective>

<execution_context>
@C:/Users/Matteo/Desktop/Progetti/bbqexperience/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Matteo/Desktop/Progetti/bbqexperience/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/STACK.md
@.planning/phases/02-design-system-frontend-scaffold/02-CONTEXT.md

Existing Astro scaffold from Phase 1:
- web/package.json has astro@^6.1.2 only
- web/astro.config.mjs has site + static output
- web/src/pages/index.astro is placeholder
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install dependencies and configure Tailwind 4 with design tokens</name>
  <files>web/package.json, web/astro.config.mjs, web/src/styles/tokens.css, web/src/styles/global.css, web/tsconfig.json</files>
  <read_first>
    - web/package.json
    - web/astro.config.mjs
    - web/src/pages/index.astro
  </read_first>
  <action>
1. Install dependencies in web/:
   - @astrojs/tailwind (or tailwindcss + @tailwindcss/vite for Tailwind 4 CSS-first approach)
   - @astrojs/sitemap
   - @fontsource-variable/inter (body font)
   - @fontsource-variable/oswald (heading font — bold, condensed, street-culture feel)
   - sharp (image optimization, likely already peer dep of Astro)

2. Update web/astro.config.mjs:
   - Add Tailwind 4 Vite plugin (import tailwindcss from '@tailwindcss/vite')
   - Add @astrojs/sitemap integration
   - Configure i18n object: defaultLocale 'en', locales ['en', 'it', 'es'], routing.prefixDefaultLocale true
   - Keep site: 'https://bbqexperience.com' and output: 'static'

3. Create web/src/styles/tokens.css with CSS custom properties (design tokens):
   ```css
   /* Token di design — BBQ Experience palette */
   :root {
     /* Colori primari — dark theme */
     --color-bg-primary: #0D0D0D;
     --color-bg-secondary: #1A1A1A;
     --color-bg-tertiary: #262626;
     --color-bg-elevated: #2D2D2D;

     /* Colori accento — fuoco/ambra/fumo */
     --color-accent-fire: #F97316;
     --color-accent-amber: #F59E0B;
     --color-accent-ember: #EA580C;
     --color-accent-smoke: #78716C;
     --color-accent-ash: #A8A29E;
     --color-accent-gold: #FCD34D;

     /* Testo */
     --color-text-primary: #F5F5F4;
     --color-text-secondary: #D6D3D1;
     --color-text-muted: #A8A29E;
     --color-text-inverse: #0D0D0D;

     /* Bordi e separatori */
     --color-border-default: #404040;
     --color-border-subtle: #2D2D2D;
     --color-border-accent: #F97316;

     /* Spaziatura (sistema a 4px) */
     --space-1: 0.25rem;
     --space-2: 0.5rem;
     --space-3: 0.75rem;
     --space-4: 1rem;
     --space-6: 1.5rem;
     --space-8: 2rem;
     --space-12: 3rem;
     --space-16: 4rem;
     --space-24: 6rem;
     --space-32: 8rem;

     /* Tipografia */
     --font-heading: 'Oswald Variable', 'Impact', sans-serif;
     --font-body: 'Inter Variable', system-ui, sans-serif;
     --font-mono: 'JetBrains Mono', monospace;

     /* Font sizes (fluid scale) */
     --text-xs: clamp(0.75rem, 0.7rem + 0.25vw, 0.875rem);
     --text-sm: clamp(0.875rem, 0.8rem + 0.375vw, 1rem);
     --text-base: clamp(1rem, 0.925rem + 0.375vw, 1.125rem);
     --text-lg: clamp(1.125rem, 1rem + 0.625vw, 1.375rem);
     --text-xl: clamp(1.25rem, 1.1rem + 0.75vw, 1.625rem);
     --text-2xl: clamp(1.5rem, 1.2rem + 1.5vw, 2.25rem);
     --text-3xl: clamp(1.875rem, 1.4rem + 2.375vw, 3rem);
     --text-4xl: clamp(2.25rem, 1.5rem + 3.75vw, 4.5rem);
     --text-5xl: clamp(3rem, 2rem + 5vw, 6rem);

     /* Raggi bordo */
     --radius-sm: 0.25rem;
     --radius-md: 0.5rem;
     --radius-lg: 0.75rem;
     --radius-xl: 1rem;
     --radius-full: 9999px;

     /* Ombre */
     --shadow-sm: 0 1px 2px rgba(0, 0, 0, 0.5);
     --shadow-md: 0 4px 6px rgba(0, 0, 0, 0.4);
     --shadow-lg: 0 10px 15px rgba(0, 0, 0, 0.3);
     --shadow-glow-fire: 0 0 20px rgba(249, 115, 22, 0.3);
     --shadow-glow-amber: 0 0 20px rgba(245, 158, 11, 0.3);

     /* Transizioni */
     --transition-fast: 150ms ease;
     --transition-base: 250ms ease;
     --transition-slow: 400ms ease;

     /* Breakpoints (riferimento, Tailwind li gestisce) */
     --bp-sm: 640px;
     --bp-md: 768px;
     --bp-lg: 1024px;
     --bp-xl: 1280px;

     /* Container */
     --container-max: 1280px;
     --container-padding: var(--space-4);
   }
   ```

4. Create web/src/styles/global.css:
   ```css
   @import "tailwindcss";
   @import "./tokens.css";

   /* Font imports */
   @import "@fontsource-variable/inter";
   @import "@fontsource-variable/oswald";

   /* Tema globale BBQ Experience */
   @layer base {
     html {
       font-family: var(--font-body);
       background-color: var(--color-bg-primary);
       color: var(--color-text-primary);
       scroll-behavior: smooth;
       -webkit-font-smoothing: antialiased;
       -moz-osx-font-smoothing: grayscale;
     }

     body {
       min-height: 100dvh;
       line-height: 1.6;
       overflow-x: hidden;
     }

     h1, h2, h3, h4, h5, h6 {
       font-family: var(--font-heading);
       font-weight: 700;
       line-height: 1.1;
       text-transform: uppercase;
       letter-spacing: 0.02em;
       color: var(--color-text-primary);
     }

     a {
       color: var(--color-accent-fire);
       text-decoration: none;
       transition: color var(--transition-fast);
     }

     a:hover {
       color: var(--color-accent-amber);
     }

     ::selection {
       background-color: var(--color-accent-fire);
       color: var(--color-text-inverse);
     }

     img {
       max-width: 100%;
       height: auto;
       display: block;
     }
   }

   /* Utilita personalizzate */
   @layer utilities {
     .container-bbq {
       max-width: var(--container-max);
       margin-inline: auto;
       padding-inline: var(--container-padding);
     }

     .text-gradient-fire {
       background: linear-gradient(135deg, var(--color-accent-fire), var(--color-accent-amber));
       -webkit-background-clip: text;
       -webkit-text-fill-color: transparent;
       background-clip: text;
     }

     .bg-gradient-dark {
       background: linear-gradient(180deg, var(--color-bg-primary) 0%, var(--color-bg-secondary) 100%);
     }

     .border-glow-fire {
       border: 1px solid var(--color-accent-fire);
       box-shadow: var(--shadow-glow-fire);
     }
   }
   ```

5. Create/update web/tsconfig.json with path aliases:
   ```json
   {
     "extends": "astro/tsconfigs/strict",
     "compilerOptions": {
       "baseUrl": ".",
       "paths": {
         "@/*": ["src/*"],
         "@components/*": ["src/components/*"],
         "@layouts/*": ["src/layouts/*"],
         "@styles/*": ["src/styles/*"],
         "@lib/*": ["src/lib/*"],
         "@i18n/*": ["src/i18n/*"]
       }
     }
   }
   ```
  </action>
  <verify>
    <automated>cd C:/Users/Matteo/Desktop/Progetti/bbqexperience/web && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - web/package.json contains "@tailwindcss/vite" or "@astrojs/tailwind" in dependencies or devDependencies
    - web/package.json contains "@astrojs/sitemap" in dependencies
    - web/package.json contains "@fontsource-variable/inter" in dependencies
    - web/package.json contains "@fontsource-variable/oswald" in dependencies
    - web/astro.config.mjs contains "i18n" configuration with "defaultLocale" and "locales"
    - web/astro.config.mjs contains "prefixDefaultLocale" set to true
    - web/src/styles/tokens.css contains "--color-bg-primary: #0D0D0D"
    - web/src/styles/tokens.css contains "--color-accent-fire: #F97316"
    - web/src/styles/tokens.css contains "--font-heading: 'Oswald Variable'"
    - web/src/styles/tokens.css contains "--font-body: 'Inter Variable'"
    - web/src/styles/global.css contains '@import "tailwindcss"'
    - web/src/styles/global.css contains '@import "./tokens.css"'
    - web/src/styles/global.css contains "background-color: var(--color-bg-primary)"
    - web/src/styles/global.css contains "font-family: var(--font-heading)"
    - web/src/styles/global.css contains ".text-gradient-fire"
    - web/tsconfig.json contains "@components/*"
    - npm run build exits with code 0
  </acceptance_criteria>
  <done>Tailwind 4 configured with Astro, design tokens defined as CSS custom properties, global styles applied with dark theme, fonts installed, tsconfig aliases set, i18n and sitemap configured in astro.config.mjs, build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Create BaseLayout, SEOHead, and OptimizedImage components</name>
  <files>web/src/layouts/BaseLayout.astro, web/src/components/common/SEOHead.astro, web/src/components/common/OptimizedImage.astro, web/src/pages/index.astro</files>
  <read_first>
    - web/astro.config.mjs
    - web/src/styles/global.css
    - web/src/styles/tokens.css
  </read_first>
  <action>
1. Create web/src/components/common/SEOHead.astro:
   - Props interface: { title: string, description: string, locale?: string, canonicalPath?: string, ogImage?: string, type?: string }
   - Default locale to 'en'
   - Generate canonical URL: `https://bbqexperience.com/{locale}{canonicalPath}`
   - Generate hreflang tags for all 3 locales (en, it, es) using a loop
   - Include x-default hreflang pointing to /en/ version
   - Include Open Graph basic meta tags (og:title, og:description, og:locale, og:type, og:url, og:site_name)
   - Include Twitter card meta (twitter:card summary_large_image)
   - Include charset, viewport, theme-color (#0D0D0D)
   - Locales array: ['en', 'it', 'es'] hardcoded for the loop

2. Create web/src/layouts/BaseLayout.astro:
   - Props interface: { title: string, description: string, locale?: string, canonicalPath?: string, ogImage?: string, class?: string }
   - Import and use SEOHead component in <head>
   - Import global.css in <head>
   - HTML lang attribute set from locale prop (default 'en')
   - Body structure: `<slot name="header" />`, `<main class={props.class}>`, `<slot />`, `</main>`, `<slot name="footer" />`
   - Add a skip-to-content link as first element in body: `<a href="#main-content" class="sr-only focus:not-sr-only ...">Skip to content</a>`
   - Main tag has id="main-content"
   - Set dir="ltr" on html (all 3 langs are LTR)

3. Create web/src/components/common/OptimizedImage.astro:
   - Props interface: { src: string | ImageMetadata, alt: string, width?: number, height?: number, widths?: number[], sizes?: string, loading?: 'lazy' | 'eager', class?: string, quality?: number }
   - Use Astro's built-in `<Image>` component from 'astro:assets'
   - Default loading to 'lazy'
   - Default quality to 80
   - Default widths to [320, 640, 960, 1280, 1920]
   - Default sizes to '(max-width: 640px) 100vw, (max-width: 1024px) 50vw, 33vw'
   - Pass format={['avif', 'webp']} to the Image component (Astro handles format negotiation)
   - Apply decoding="async" attribute

4. Update web/src/pages/index.astro to use BaseLayout:
   - Import BaseLayout from '@layouts/BaseLayout.astro'
   - Wrap content in BaseLayout with title="BBQ Experience" description="The ultimate BBQ product review destination" locale="en"
   - Add a simple hero section with dark background to verify the design system works:
     - h1 with font-heading (Oswald), text-gradient-fire utility: "BBQ EXPERIENCE"
     - p with text-secondary color: "The most complete BBQ review destination online."
     - Centered on page, using container-bbq class
     - Min-h-screen, flex center layout
  </action>
  <verify>
    <automated>cd C:/Users/Matteo/Desktop/Progetti/bbqexperience/web && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - web/src/layouts/BaseLayout.astro contains "SEOHead" import
    - web/src/layouts/BaseLayout.astro contains 'import "@styles/global.css"' or 'import "../styles/global.css"'
    - web/src/layouts/BaseLayout.astro contains '<html lang='
    - web/src/layouts/BaseLayout.astro contains 'id="main-content"'
    - web/src/components/common/SEOHead.astro contains "hreflang"
    - web/src/components/common/SEOHead.astro contains "canonical"
    - web/src/components/common/SEOHead.astro contains "#0D0D0D" (theme-color)
    - web/src/components/common/SEOHead.astro contains "og:title"
    - web/src/components/common/OptimizedImage.astro contains 'from "astro:assets"' or "from 'astro:assets'"
    - web/src/components/common/OptimizedImage.astro contains 'loading'
    - web/src/components/common/OptimizedImage.astro contains 'decoding="async"'
    - web/src/pages/index.astro contains "BaseLayout"
    - web/src/pages/index.astro contains "BBQ EXPERIENCE"
    - npm run build exits with code 0
  </acceptance_criteria>
  <done>BaseLayout renders with dark theme (#0D0D0D background), hreflang tags for en/it/es, canonical URL, responsive viewport meta, Oswald headings, Inter body text. OptimizedImage wraps Astro Image with lazy loading and WebP/AVIF. Index page renders with fire gradient heading.</done>
</task>

</tasks>

<verification>
- `cd web && npm run build` completes without errors
- Built HTML in dist/en/index.html contains hreflang tags for en, it, es
- Built HTML contains canonical link
- Built HTML contains theme-color #0D0D0D
- Built CSS includes the design token custom properties
</verification>

<success_criteria>
- Dark theme design system is configured with exact hex values (#0D0D0D, #1A1A1A, #F97316, #F59E0B)
- BaseLayout produces valid HTML with hreflang and canonical tags for all 3 locales
- OptimizedImage component exists with lazy loading, WebP/AVIF support, responsive srcset
- Tailwind 4 is integrated and build passes
- Oswald (headings) and Inter (body) fonts are installed and referenced in CSS
</success_criteria>

<output>
After completion, create `.planning/phases/02-design-system-frontend-scaffold/02-01-SUMMARY.md`
</output>
