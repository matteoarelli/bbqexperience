---
phase: 02-design-system-frontend-scaffold
plan: 03
type: execute
wave: 2
depends_on: [02-01, 02-02]
files_modified:
  - web/src/components/common/Header.astro
  - web/src/components/common/Footer.astro
  - web/src/components/common/Nav.astro
  - web/src/components/common/MobileMenu.astro
  - web/src/components/common/Hero.astro
  - web/src/lib/animations.ts
  - web/src/pages/en/index.astro
  - web/src/pages/it/index.astro
  - web/src/pages/es/index.astro
autonomous: false
requirements: [DES-01, DES-02, DES-03]

must_haves:
  truths:
    - "Site has a visible header with logo, navigation links, and language switcher"
    - "Site has a footer with sections, social links, and copyright"
    - "Homepage hero section has bold BBQ typography with scroll-triggered animation"
    - "Navigation works on mobile with hamburger menu"
    - "Scroll-triggered animations play smoothly without layout shifts"
  artifacts:
    - path: "web/src/components/common/Header.astro"
      provides: "Site header with logo, Nav, LanguageSwitcher"
      contains: "Nav"
      min_lines: 20
    - path: "web/src/components/common/Footer.astro"
      provides: "Site footer with sections, social, copyright"
      contains: "Instagram"
      min_lines: 30
    - path: "web/src/components/common/Nav.astro"
      provides: "Desktop navigation with localized links"
      contains: "localizedRoutes"
    - path: "web/src/components/common/MobileMenu.astro"
      provides: "Mobile hamburger menu with slide-in panel"
      contains: "hamburger"
    - path: "web/src/components/common/Hero.astro"
      provides: "Homepage hero with animated title"
      contains: "gsap"
    - path: "web/src/lib/animations.ts"
      provides: "GSAP animation utilities (fadeInUp, staggerReveal, etc.)"
      exports: ["initScrollAnimations", "fadeInUp"]
  key_links:
    - from: "web/src/components/common/Header.astro"
      to: "web/src/components/common/Nav.astro"
      via: "component import"
      pattern: "Nav"
    - from: "web/src/components/common/Header.astro"
      to: "web/src/components/common/LanguageSwitcher.astro"
      via: "component import"
      pattern: "LanguageSwitcher"
    - from: "web/src/components/common/Hero.astro"
      to: "web/src/lib/animations.ts"
      via: "script import for GSAP"
      pattern: "gsap"
    - from: "web/src/pages/en/index.astro"
      to: "web/src/layouts/BaseLayout.astro"
      via: "layout import"
      pattern: "BaseLayout"
---

<objective>
Build the UI shell (Header, Footer, Nav, Hero) with GSAP scroll animations and wire all locale pages to use BaseLayout with full design system. This is where the site transforms from a placeholder into a bold BBQ editorial experience.

Purpose: Deliver the visible "wow factor" — dark theme, fire accents, animated hero, responsive mobile nav — that defines BBQ Experience's brand identity.
Output: Complete UI shell with animated homepage for all 3 locales.
</objective>

<execution_context>
@C:/Users/Matteo/Desktop/Progetti/bbqexperience/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Matteo/Desktop/Progetti/bbqexperience/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/STACK.md
@.planning/phases/02-design-system-frontend-scaffold/02-CONTEXT.md
@.planning/phases/02-design-system-frontend-scaffold/02-01-SUMMARY.md
@.planning/phases/02-design-system-frontend-scaffold/02-02-SUMMARY.md

<interfaces>
<!-- Contratti da Plan 01 e Plan 02 che questo piano consuma -->

From web/src/layouts/BaseLayout.astro (Plan 01):
Props: { title: string, description: string, locale?: string, canonicalPath?: string, ogImage?: string, class?: string }
Slots: "header" (named), default, "footer" (named)

From web/src/components/common/SEOHead.astro (Plan 01):
Props: { title: string, description: string, locale?: string, canonicalPath?: string, ogImage?: string, type?: string }

From web/src/components/common/OptimizedImage.astro (Plan 01):
Props: { src: string | ImageMetadata, alt: string, width?: number, height?: number, loading?: 'lazy' | 'eager', class?: string }

From web/src/lib/i18n.ts (Plan 02):
export const defaultLocale = 'en';
export const locales = ['en', 'it', 'es'];
export const localeNames: Record<Locale, string>;
export const localizedRoutes: Record<string, Record<Locale, string>>;
export function getLocaleFromPath(path: string): Locale;
export function getLocalizedPath(currentPath: string, targetLocale: Locale): string;
export function loadTranslations(locale: Locale): Promise<Record<string, string>>;
export function getTranslation(translations: Record<string, any>, key: string): string;

From web/src/components/common/LanguageSwitcher.astro (Plan 02):
Props: { currentLocale: string, currentPath: string }

Design tokens from web/src/styles/tokens.css (Plan 01):
--color-bg-primary: #0D0D0D
--color-bg-secondary: #1A1A1A
--color-bg-tertiary: #262626
--color-accent-fire: #F97316
--color-accent-amber: #F59E0B
--color-accent-ember: #EA580C
--color-accent-gold: #FCD34D
--color-text-primary: #F5F5F4
--color-text-secondary: #D6D3D1
--color-text-muted: #A8A29E
--font-heading: 'Oswald Variable'
--font-body: 'Inter Variable'
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Header, Nav, MobileMenu, Footer, and GSAP animation utilities</name>
  <files>web/src/components/common/Header.astro, web/src/components/common/Nav.astro, web/src/components/common/MobileMenu.astro, web/src/components/common/Footer.astro, web/src/lib/animations.ts</files>
  <read_first>
    - web/src/layouts/BaseLayout.astro
    - web/src/components/common/LanguageSwitcher.astro
    - web/src/lib/i18n.ts
    - web/src/styles/tokens.css
    - web/package.json
  </read_first>
  <action>
1. Install GSAP and Lenis in web/:
   ```
   npm install gsap lenis
   ```

2. Create web/src/lib/animations.ts — GSAP utility module:
   ```typescript
   // Utilita per animazioni GSAP — BBQ Experience
   // REGOLA: solo transform e opacity per performance (no layout properties)
   import { gsap } from 'gsap';
   import { ScrollTrigger } from 'gsap/ScrollTrigger';

   gsap.registerPlugin(ScrollTrigger);

   /**
    * Inizializza animazioni scroll-triggered su elementi con data-animate
    * Ogni elemento con data-animate viene animato con fadeInUp al scroll
    */
   export function initScrollAnimations(): void {
     const elements = document.querySelectorAll('[data-animate]');

     elements.forEach((el) => {
       const delay = parseFloat(el.getAttribute('data-animate-delay') || '0');
       const duration = parseFloat(el.getAttribute('data-animate-duration') || '0.8');

       gsap.from(el, {
         scrollTrigger: {
           trigger: el,
           start: 'top 85%',
           toggleActions: 'play none none none',
         },
         y: 40,
         opacity: 0,
         duration,
         delay,
         ease: 'power3.out',
       });
     });
   }

   /**
    * Fade in dal basso — per singoli elementi
    */
   export function fadeInUp(selector: string, options?: { delay?: number; duration?: number; y?: number }): void {
     gsap.from(selector, {
       scrollTrigger: {
         trigger: selector,
         start: 'top 85%',
       },
       y: options?.y ?? 40,
       opacity: 0,
       duration: options?.duration ?? 0.8,
       delay: options?.delay ?? 0,
       ease: 'power3.out',
     });
   }

   /**
    * Reveal scaglionato — per liste di elementi
    */
   export function staggerReveal(parentSelector: string, childSelector: string, stagger: number = 0.1): void {
     gsap.from(`${parentSelector} ${childSelector}`, {
       scrollTrigger: {
         trigger: parentSelector,
         start: 'top 80%',
       },
       y: 30,
       opacity: 0,
       duration: 0.6,
       stagger,
       ease: 'power2.out',
     });
   }

   /**
    * Animazione titolo con effetto di scrittura da sinistra
    */
   export function titleReveal(selector: string): void {
     gsap.from(selector, {
       scrollTrigger: {
         trigger: selector,
         start: 'top 85%',
       },
       x: -60,
       opacity: 0,
       duration: 1,
       ease: 'power4.out',
     });
   }
   ```

3. Create web/src/components/common/Nav.astro:
   - Props: { locale: string, translations: Record<string, any> }
   - Import getTranslation from '@lib/i18n' and localizedRoutes
   - Render a <nav> with role="navigation" aria-label="Main navigation"
   - Navigation links: Reviews, Recipes, Tutorials, Blog
   - Each link href uses localizedRoutes mapping: `/${locale}/${localizedRoutes[key][locale]}/`
   - Display text from translations via getTranslation(translations, `nav.${key}`)
   - Style: flex row, gap-8, uppercase, font-semibold, text-sm, tracking-wider
   - Link color: var(--color-text-secondary) (#D6D3D1), hover: var(--color-accent-fire) (#F97316)
   - Transition on hover: color transition-fast (150ms)
   - Hidden on mobile (hidden lg:flex)

4. Create web/src/components/common/MobileMenu.astro:
   - Props: { locale: string, translations: Record<string, any>, currentPath: string }
   - Import localizedRoutes, locales, localeNames, getLocalizedPath from '@lib/i18n'
   - Hamburger button: 3 horizontal bars (spans), positioned fixed top-right on mobile, z-50
     - Bars: width 24px, height 2px, background var(--color-text-primary) (#F5F5F4), gap 6px
     - Use aria-label="Menu", aria-expanded="false"
   - Slide-in panel: full-screen overlay, background var(--color-bg-primary) (#0D0D0D) with opacity 0.98
     - Position fixed, inset-0, z-40, transform translateX(100%) by default
     - When open: translateX(0) with transition 300ms ease
   - Panel content: nav links (same as Nav but vertical, text-2xl, uppercase, Oswald font)
   - Include language switcher links at bottom of panel
   - Client-side script (inline <script>):
     - Toggle open/close on hamburger click
     - Set aria-expanded
     - Close on link click
     - Close on Escape key
     - Prevent body scroll when open (overflow hidden on body)
   - Only visible on mobile (lg:hidden for the hamburger button)

5. Create web/src/components/common/Header.astro:
   - Props: { locale: string, translations: Record<string, any>, currentPath: string }
   - Import Nav, LanguageSwitcher, MobileMenu
   - Structure: <header> with sticky top-0, z-30, backdrop-blur-md, bg-[#0D0D0D]/80
   - Inner container: container-bbq, flex justify-between items-center, h-16 (mobile) lg:h-20
   - Left: Logo text "BBQ" in Oswald bold uppercase, color var(--color-accent-fire) (#F97316), followed by "EXP" in var(--color-text-primary) (#F5F5F4). Link to `/${locale}/`
   - Center (desktop only): Nav component
   - Right (desktop): LanguageSwitcher (position relative, not fixed — override Plan 02's fixed positioning)
   - Right (mobile): MobileMenu hamburger
   - Add a bottom border: 1px solid var(--color-border-subtle) (#2D2D2D)

6. Create web/src/components/common/Footer.astro:
   - Props: { locale: string, translations: Record<string, any> }
   - Import getTranslation from '@lib/i18n' and localizedRoutes
   - Structure: <footer> with bg-[#0D0D0D], border-t border-[#2D2D2D], pt-16 pb-8
   - Inner: container-bbq
   - Grid layout: 1 col mobile, 4 cols lg
     - Col 1: Logo "BBQ EXP" + about text from translations (footer.about)
     - Col 2: Sections header (footer.sections) + links to Reviews, Recipes, Tutorials, Blog
     - Col 3: Connect header (footer.connect) + Instagram link (https://instagram.com/bbqexperience), "Follow us" CTA
     - Col 4: Legal header (footer.legal) + placeholder links (Privacy, Terms)
   - Bottom bar: border-t border-[#2D2D2D], mt-12, pt-8, flex justify-between
     - Left: copyright text with current year
     - Right: "Made with fire" text in var(--color-accent-fire) (#F97316)
   - All section headers: Oswald font, uppercase, text-sm, tracking-wider, color var(--color-accent-fire) (#F97316), mb-4
   - Links: color var(--color-text-muted) (#A8A29E), hover var(--color-text-primary) (#F5F5F4), transition 150ms
  </action>
  <verify>
    <automated>cd C:/Users/Matteo/Desktop/Progetti/bbqexperience/web && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - web/package.json contains "gsap" in dependencies
    - web/package.json contains "lenis" in dependencies
    - web/src/lib/animations.ts contains "export function initScrollAnimations"
    - web/src/lib/animations.ts contains "export function fadeInUp"
    - web/src/lib/animations.ts contains "export function staggerReveal"
    - web/src/lib/animations.ts contains "ScrollTrigger"
    - web/src/lib/animations.ts contains "gsap.registerPlugin"
    - web/src/components/common/Nav.astro contains "localizedRoutes"
    - web/src/components/common/Nav.astro contains 'role="navigation"'
    - web/src/components/common/Nav.astro contains "lg:flex"
    - web/src/components/common/MobileMenu.astro contains 'aria-label="Menu"'
    - web/src/components/common/MobileMenu.astro contains 'aria-expanded'
    - web/src/components/common/MobileMenu.astro contains "translateX"
    - web/src/components/common/MobileMenu.astro contains "lg:hidden"
    - web/src/components/common/Header.astro contains "Nav"
    - web/src/components/common/Header.astro contains "LanguageSwitcher"
    - web/src/components/common/Header.astro contains "MobileMenu"
    - web/src/components/common/Header.astro contains "backdrop-blur"
    - web/src/components/common/Header.astro contains "sticky"
    - web/src/components/common/Header.astro contains "#F97316" or "accent-fire"
    - web/src/components/common/Footer.astro contains "Instagram"
    - web/src/components/common/Footer.astro contains "footer.about" or "getTranslation"
    - web/src/components/common/Footer.astro contains "border-t"
  </acceptance_criteria>
  <done>Header with sticky dark backdrop, logo, desktop nav, mobile hamburger menu. Footer with 4-column grid, social links, copyright. Nav links use localized routes. GSAP animation utilities ready for scroll-triggered effects. All components use exact design token colors.</done>
</task>

<task type="auto">
  <name>Task 2: Create Hero component and wire all locale pages to BaseLayout with full UI shell</name>
  <files>web/src/components/common/Hero.astro, web/src/pages/en/index.astro, web/src/pages/it/index.astro, web/src/pages/es/index.astro</files>
  <read_first>
    - web/src/layouts/BaseLayout.astro
    - web/src/components/common/Header.astro
    - web/src/components/common/Footer.astro
    - web/src/lib/i18n.ts
    - web/src/i18n/en.json
    - web/src/lib/animations.ts
  </read_first>
  <action>
1. Create web/src/components/common/Hero.astro:
   - Props: { title: string, subtitle: string, ctaText: string, ctaHref: string, locale: string }
   - Structure: <section> with min-h-[90vh], flex items-center justify-center, relative, overflow-hidden
   - Background: bg-gradient-dark (from tokens), with a subtle radial gradient overlay:
     ```
     background: radial-gradient(ellipse at 50% 0%, rgba(249,115,22,0.15) 0%, transparent 60%),
                 linear-gradient(180deg, #0D0D0D 0%, #1A1A1A 100%);
     ```
   - Content container: container-bbq, text-center, relative z-10
   - Title: <h1> with font-heading (Oswald), text-5xl token size (clamp 3rem-6rem), uppercase, font-black
     - "BBQ" in text-gradient-fire utility (orange-amber gradient)
     - "EXPERIENCE" in var(--color-text-primary) (#F5F5F4)
     - Add data-animate attribute for scroll trigger
     - letter-spacing: -0.02em for tight display
   - Subtitle: <p> with text-xl token size, color var(--color-text-secondary) (#D6D3D1), max-w-2xl mx-auto, mt-6
     - Add data-animate with data-animate-delay="0.2"
   - CTA button: <a> with inline-block, mt-8, px-8 py-4, bg-[#F97316] hover:bg-[#EA580C], text-[#0D0D0D] font-bold uppercase tracking-wider, rounded-lg, transition 250ms
     - Add data-animate with data-animate-delay="0.4"
     - Text shadow for readability on dark sections
   - Decorative element: a horizontal line (div) below CTA, w-24 h-1, bg-gradient fire-to-amber, mx-auto mt-12, data-animate delay 0.6
   - Client-side script block:
     ```
     <script>
       import { initScrollAnimations } from '@lib/animations';
       // Inizializza le animazioni di scroll sulla hero
       document.addEventListener('DOMContentLoaded', () => {
         initScrollAnimations();
       });
     </script>
     ```
     Note: If @lib/ alias doesn't work in client scripts, use relative path '../../lib/animations'

2. Rewrite web/src/pages/en/index.astro to use full layout:
   ```astro
   ---
   // Homepage inglese — BBQ Experience
   import BaseLayout from '@layouts/BaseLayout.astro';
   import Header from '@components/common/Header.astro';
   import Footer from '@components/common/Footer.astro';
   import Hero from '@components/common/Hero.astro';
   import { loadTranslations, getTranslation } from '@lib/i18n';
   import type { Locale } from '@lib/i18n';

   const locale: Locale = 'en';
   const translations = await loadTranslations(locale);
   const t = (key: string) => getTranslation(translations, key);
   ---

   <BaseLayout
     title={`${t('site.name')} - ${t('site.tagline')}`}
     description={t('site.description')}
     locale={locale}
     canonicalPath="/"
   >
     <Header slot="header" locale={locale} translations={translations} currentPath={`/${locale}/`} />

     <Hero
       title={t('hero.title')}
       subtitle={t('hero.subtitle')}
       ctaText={t('hero.cta')}
       ctaHref={`/${locale}/reviews/`}
       locale={locale}
     />

     <!-- Sezione placeholder per contenuti futuri -->
     <section class="container-bbq py-24" data-animate>
       <h2 class="text-3xl font-heading uppercase text-center" style="font-family: var(--font-heading); font-size: var(--text-3xl);">
         {t('common.loading')}
       </h2>
       <p class="text-center mt-4" style="color: var(--color-text-muted);">
         Content sections will be added in future phases.
       </p>
     </section>

     <Footer slot="footer" locale={locale} translations={translations} />
   </BaseLayout>
   ```

3. Rewrite web/src/pages/it/index.astro — same as en but locale='it'

4. Rewrite web/src/pages/es/index.astro — same as en but locale='es'

5. Verify that the build produces all 3 locale pages with full layout (header, hero, footer).
  </action>
  <verify>
    <automated>cd C:/Users/Matteo/Desktop/Progetti/bbqexperience/web && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - web/src/components/common/Hero.astro contains "min-h-[90vh]" or "min-height: 90vh"
    - web/src/components/common/Hero.astro contains "BBQ" text rendering
    - web/src/components/common/Hero.astro contains "data-animate"
    - web/src/components/common/Hero.astro contains "gsap" or "initScrollAnimations" or "animations"
    - web/src/components/common/Hero.astro contains "#F97316" or "accent-fire"
    - web/src/components/common/Hero.astro contains "radial-gradient"
    - web/src/pages/en/index.astro contains "BaseLayout"
    - web/src/pages/en/index.astro contains "Header"
    - web/src/pages/en/index.astro contains "Footer"
    - web/src/pages/en/index.astro contains "Hero"
    - web/src/pages/it/index.astro contains "BaseLayout"
    - web/src/pages/it/index.astro contains "locale" assigned to "it"
    - web/src/pages/es/index.astro contains "BaseLayout"
    - web/src/pages/es/index.astro contains "locale" assigned to "es"
    - npm run build exits with code 0
  </acceptance_criteria>
  <done>All 3 locale homepages render with full UI shell: sticky header with logo + nav + language switcher, animated hero with BBQ EXPERIENCE title in fire gradient, CTA button, and footer with sections grid. GSAP scroll animations trigger on viewport entry. Mobile hamburger menu available below lg breakpoint.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of design system</name>
  <what-built>Complete BBQ Experience design system with dark theme, animated hero, responsive header/footer, and multilingual routing across 3 locales.</what-built>
  <how-to-verify>
    1. Run `cd web && npm run dev` to start the dev server
    2. Visit http://localhost:4321/en/ — verify:
       - Dark background (#0D0D0D) is visible
       - "BBQ EXPERIENCE" heading uses bold condensed font (Oswald) with fire-orange gradient
       - Subtitle text is light gray (#D6D3D1)
       - CTA button is orange (#F97316) with dark text
       - Header is sticky with "BBQ EXP" logo and navigation links
       - Footer has 4-column layout with sections
    3. Scroll down — verify hero elements animate in (fade up from below)
    4. Visit http://localhost:4321/it/ — verify Italian translations appear
    5. Visit http://localhost:4321/es/ — verify Spanish translations appear
    6. Click language links in header — verify they switch between locales
    7. Resize browser to mobile width (~375px) — verify:
       - Hamburger menu appears (3 horizontal bars)
       - Desktop nav is hidden
       - Tapping hamburger opens full-screen menu
       - Menu has nav links and language options
       - Layout doesn't break, no horizontal scroll
    8. Visit http://localhost:4321/ — verify it redirects to /en/
  </how-to-verify>
  <resume-signal>Type "approved" if the design looks correct, or describe any issues to fix.</resume-signal>
</task>

</tasks>

<verification>
- `cd web && npm run build` succeeds
- dist/en/index.html contains: header, nav, hero section, footer, hreflang tags, canonical
- dist/it/index.html contains Italian text and lang="it"
- dist/es/index.html contains Spanish text and lang="es"
- GSAP and Lenis are in package.json dependencies
- All components use design tokens (#0D0D0D, #F97316, Oswald, Inter)
</verification>

<success_criteria>
- Bold BBQ design is visually verified: dark theme, fire accents, Oswald headings, animated hero
- Responsive layout works on mobile with hamburger menu
- All 3 locale pages render with correct translations and full UI shell
- Scroll animations trigger smoothly without layout shifts
- Header is sticky with backdrop blur on scroll
- Footer has complete structure with social links and sections
</success_criteria>

<output>
After completion, create `.planning/phases/02-design-system-frontend-scaffold/02-03-SUMMARY.md`
</output>
