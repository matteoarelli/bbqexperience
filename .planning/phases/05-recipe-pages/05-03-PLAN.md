---
phase: 05-recipe-pages
plan: 03
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - web/src/components/recipe/PrintRecipeCard.astro
  - web/src/styles/print.css
  - web/src/pages/en/recipes/[slug].astro
  - web/src/pages/it/ricette/[slug].astro
  - web/src/pages/es/recetas/[slug].astro
autonomous: true
requirements: [REC-06]

must_haves:
  truths:
    - "User can print a clean recipe card with ingredients and steps only"
    - "Printed card includes a QR code linking back to the full recipe page"
    - "Print layout hides navigation, interactive controls, editorial intro"
  artifacts:
    - path: "web/src/styles/print.css"
      provides: "Print-specific CSS hiding UI chrome"
      contains: "@media print"
    - path: "web/src/components/recipe/PrintRecipeCard.astro"
      provides: "Print button and QR code"
      contains: "window.print"
  key_links:
    - from: "web/src/styles/print.css"
      to: "recipe page sections"
      via: "@media print display:none rules"
      pattern: "@media print.*display.*none"
---

<objective>
Add print functionality to recipe pages: a print button that triggers a clean recipe card layout (ingredients + steps only) with a QR code linking back to the full page.

Purpose: Cooks often print recipes to use in the kitchen. A clean print layout with QR code drives return traffic to the site.
Output: Print stylesheet and print button component with QR code generation.
</objective>

<execution_context>
@C:/Users/Matteo/Desktop/Progetti/bbqexperience/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Matteo/Desktop/Progetti/bbqexperience/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-recipe-pages/05-01-SUMMARY.md

@web/src/styles/tokens.css
@web/src/pages/en/recipes/[slug].astro
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create print stylesheet and PrintRecipeCard component</name>
  <files>
    web/src/styles/print.css,
    web/src/components/recipe/PrintRecipeCard.astro
  </files>
  <action>
    read_first: web/src/styles/global.css, web/src/styles/tokens.css, web/src/pages/en/recipes/[slug].astro

    1. Create `web/src/styles/print.css` with `@media print` rules:
       - Hide elements with class `.no-print`: `display: none !important`
       - Elements to mark as `.no-print` in recipe pages: header, footer, nav, JumpToRecipe button, CookMode button, ServingAdjuster controls, UnitToggle controls, preview banner, editorial intro section
       - Show elements with class `.print-only`: normally `display: none`, in print `display: block`
       - Reset background to white, text to black for ink-friendly printing
       - Body: `background: white; color: black; font-size: 12pt;`
       - Remove shadows, borders, rounded corners for cleaner print
       - Images: `max-width: 100%; page-break-inside: avoid;`
       - Recipe card section (`#recipe-card`): `page-break-before: avoid;`
       - Instructions: `page-break-inside: avoid;` per step
       - QR code area (`.print-qr`): show only in print, positioned at bottom-right of last page
       - Set link text decoration to none, don't expand URLs

    2. Create `web/src/components/recipe/PrintRecipeCard.astro`:
       - Props: `recipeUrl` (string, full canonical URL), `label` (string, default "Print Recipe")
       - Print button: styled with `var(--color-bg-tertiary)` bg, `var(--color-text-primary)` text, printer icon (inline SVG), class `no-print`
       - On click: `window.print()`
       - QR code section (class `print-only print-qr`):
         - Generate QR code as inline SVG using a minimal QR code generator
         - Do NOT add an npm dependency for QR. Instead, use an `<img>` tag pointing to a QR code API:
           `https://api.qrserver.com/v1/create-qr-code/?size=100x100&data={encodedUrl}`
         - Below QR: small text "Scan for full recipe" and the URL in small font
         - The QR `<img>` has `loading="lazy"` since it's hidden by default and only loads for print

    3. Import `print.css` in the recipe pages:
       - Add `import '@styles/print.css';` to the frontmatter of each recipe page
       - Mark sections with appropriate `.no-print` classes:
         - Editorial intro wrapper: add `no-print` class
         - JumpToRecipe component wrapper: add `no-print` class
         - The Svelte island control bars (ServingAdjuster buttons, UnitToggle switch): these are inside Svelte components, so wrap the islands in a `<div>` and add `no-print` to the control wrapper only (ingredient list itself should print)
       - Add PrintRecipeCard component to each recipe page, positioned near the recipe card header
       - Pass the full canonical URL: `recipeUrl={`https://bbqexperience.com/${locale}/recipes/${recipe.slug}`}` (use localized route segment)
       - Translate label per locale: EN "Print Recipe", IT "Stampa Ricetta", ES "Imprimir Receta"
  </action>
  <verify>
    <automated>cd C:/Users/Matteo/Desktop/Progetti/bbqexperience/web && npx astro check 2>&1 | tail -20</automated>
  </verify>
  <done>
    Print button visible on recipe pages. Clicking it opens browser print dialog with clean layout: white background, ingredients + steps only, QR code at bottom. Navigation, editorial intro, interactive controls are hidden in print view.
  </done>
</task>

</tasks>

<verification>
- `npx astro check` passes with no errors
- `npx astro build` succeeds
- Print stylesheet correctly hides `.no-print` elements and shows `.print-only` elements
- QR code image URL is correctly encoded with the recipe's canonical URL
- Print layout shows recipe title, ingredients, instructions, and QR code on white background
</verification>

<success_criteria>
- Print button triggers browser print dialog
- Printed output contains only: recipe title, ingredients list, step-by-step instructions, QR code
- QR code links to the full recipe page URL with correct locale path
- All navigation, interactive controls, editorial intro hidden in print
- Print layout uses black text on white background for ink efficiency
</success_criteria>

<output>
After completion, create `.planning/phases/05-recipe-pages/05-03-SUMMARY.md`
</output>
