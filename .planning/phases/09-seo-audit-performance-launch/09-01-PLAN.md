---
phase: 09-seo-audit-performance-launch
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/components/common/BreadcrumbJsonLd.astro
  - web/src/components/content/Breadcrumbs.astro
  - web/src/pages/en/reviews/[slug].astro
  - web/src/pages/it/recensioni/[slug].astro
  - web/src/pages/es/resenas/[slug].astro
  - web/src/pages/en/tutorials/[slug].astro
  - web/src/pages/it/guide/[slug].astro
  - web/src/pages/es/tutoriales/[slug].astro
  - web/src/pages/en/blog/[slug].astro
  - web/src/pages/it/blog/[slug].astro
  - web/src/pages/es/blog/[slug].astro
  - web/src/pages/en/recipes/[slug].astro
  - web/src/pages/it/ricette/[slug].astro
  - web/src/pages/es/recetas/[slug].astro
  - web/src/pages/en/index.astro
  - web/src/pages/it/index.astro
  - web/src/pages/es/index.astro
  - web/astro.config.mjs
autonomous: true
requirements: [SEO-03, SEO-04, SEO-05]

must_haves:
  truths:
    - "Every page outputs BreadcrumbList Schema.org JSON-LD in the HTML head"
    - "All content pages (reviews, recipes, tutorials, blog) pass ogImage to SEOHead for social sharing previews"
    - "Per-locale XML sitemaps are generated at build time with correct hreflang entries"
  artifacts:
    - path: "web/src/components/common/BreadcrumbJsonLd.astro"
      provides: "BreadcrumbList Schema.org JSON-LD component"
      exports: ["default"]
    - path: "web/astro.config.mjs"
      provides: "Sitemap integration with i18n locale config"
      contains: "i18n"
  key_links:
    - from: "web/src/pages/en/reviews/[slug].astro"
      to: "web/src/components/common/BreadcrumbJsonLd.astro"
      via: "import and render with breadcrumb items"
      pattern: "BreadcrumbJsonLd"
    - from: "web/src/pages/en/reviews/[slug].astro"
      to: "web/src/components/common/SEOHead.astro"
      via: "ogImage prop on BaseLayout"
      pattern: "ogImage="
---

<objective>
Add BreadcrumbList Schema.org JSON-LD to all pages, fix missing ogImage on non-recipe content pages, and configure per-locale XML sitemaps.

Purpose: Complete structured data coverage (SEO-04), social sharing previews (SEO-05), and sitemap generation (SEO-03) for launch readiness.
Output: BreadcrumbJsonLd component, updated pages with ogImage, configured sitemap integration.
</objective>

<execution_context>
@C:/Users/Matteo/Desktop/Progetti/bbqexperience/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Matteo/Desktop/Progetti/bbqexperience/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Existing BreadcrumbItem type used in Breadcrumbs.astro -->
From web/src/components/content/Breadcrumbs.astro:
```typescript
interface BreadcrumbItem {
  label: string;
  href?: string;
}
```

From web/src/components/common/SEOHead.astro:
```typescript
interface Props {
  title: string;
  description: string;
  locale?: string;
  canonicalPath?: string;
  ogImage?: string;
  type?: string;
}
```

From web/src/layouts/BaseLayout.astro:
```typescript
interface Props {
  title: string;
  description: string;
  locale?: string;
  canonicalPath?: string;
  ogImage?: string;
  class?: string;
}
```

Pages already passing ogImage (ONLY recipes):
- web/src/pages/en/recipes/[slug].astro → ogImage={coverImageUrl || ''}
- web/src/pages/it/ricette/[slug].astro → ogImage={coverImageUrl || ''}
- web/src/pages/es/recetas/[slug].astro → ogImage={coverImageUrl || ''}

Pages MISSING ogImage (need fix):
- All review pages (en/it/es) — have coverImage var but don't pass it
- All tutorial pages (en/it/es) — have coverImageUrl var but don't pass it
- All blog pages (en/it/es) — have coverImageUrl var but don't pass it
- All homepage pages (en/it/es) — no dynamic image, use a default OG image

Pages with Breadcrumbs component (already have breadcrumbItems array):
- All tutorial [slug] pages (en/it/es)
- All blog [slug] pages (en/it/es)
- Tutorial listing pages (en/it/es)
- Blog listing pages (en/it/es)

Pages WITHOUT Breadcrumbs (need breadcrumb items added):
- Review [slug] pages (en/it/es)
- Recipe [slug] pages (en/it/es)
- Homepage pages (en/it/es) — no breadcrumbs needed (root)
- Compare pages (en/it/es)

Sitemap: @astrojs/sitemap already installed and in integrations array.
astro.config.mjs has i18n config with locales ['en', 'it', 'es'] and prefixDefaultLocale: true.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create BreadcrumbJsonLd component and configure per-locale sitemaps</name>
  <files>web/src/components/common/BreadcrumbJsonLd.astro, web/astro.config.mjs</files>
  <action>
    read_first:
    - web/src/components/content/Breadcrumbs.astro (for BreadcrumbItem interface)
    - web/src/components/review/ReviewJsonLd.astro (for JSON-LD pattern)
    - web/astro.config.mjs (current sitemap config)

    1. Create `web/src/components/common/BreadcrumbJsonLd.astro`:
       - Accept props: `items: BreadcrumbItem[]` (same interface as Breadcrumbs.astro — `{ label: string; href?: string }`) and `siteUrl?: string` (default `'https://bbqexperience.com'`).
       - Generate a `<script type="application/ld+json">` block with Schema.org BreadcrumbList.
       - Map each item to an `itemListElement` with `@type: ListItem`, `position` (1-indexed), `name: item.label`, and `item: siteUrl + item.href` (only if item.href exists — last item is current page, omit `item` URL for it per Google guidelines).
       - Use `set:html={JSON.stringify(jsonLd)}` pattern (same as ReviewJsonLd.astro).
       - Add Italian comment header matching project convention.

    2. Update `web/astro.config.mjs` sitemap config:
       - Change `sitemap()` to `sitemap({ i18n: { defaultLocale: 'en', locales: { en: 'en', it: 'it', es: 'es' } } })` so the generated sitemap includes hreflang alternate links between locale versions.
       - Keep all other config unchanged.

    acceptance_criteria:
    - `grep -c "BreadcrumbList" web/src/components/common/BreadcrumbJsonLd.astro` returns >= 1
    - `grep -c "itemListElement" web/src/components/common/BreadcrumbJsonLd.astro` returns >= 1
    - `grep -c "i18n" web/astro.config.mjs` returns >= 2 (existing i18n block + sitemap i18n)
  </action>
  <verify>
    <automated>cd C:/Users/Matteo/Desktop/Progetti/bbqexperience/web && grep -c "BreadcrumbList" src/components/common/BreadcrumbJsonLd.astro && grep -c "itemListElement" src/components/common/BreadcrumbJsonLd.astro && grep "i18n" astro.config.mjs | head -5</automated>
  </verify>
  <done>BreadcrumbJsonLd.astro exists with valid BreadcrumbList JSON-LD output. astro.config.mjs sitemap integration includes i18n locale mapping for en/it/es.</done>
</task>

<task type="auto">
  <name>Task 2: Add BreadcrumbJsonLd and ogImage to all content pages across 3 locales</name>
  <files>
    web/src/pages/en/reviews/[slug].astro, web/src/pages/it/recensioni/[slug].astro, web/src/pages/es/resenas/[slug].astro,
    web/src/pages/en/tutorials/[slug].astro, web/src/pages/it/guide/[slug].astro, web/src/pages/es/tutoriales/[slug].astro,
    web/src/pages/en/blog/[slug].astro, web/src/pages/it/blog/[slug].astro, web/src/pages/es/blog/[slug].astro,
    web/src/pages/en/recipes/[slug].astro, web/src/pages/it/ricette/[slug].astro, web/src/pages/es/recetas/[slug].astro,
    web/src/pages/en/index.astro, web/src/pages/it/index.astro, web/src/pages/es/index.astro
  </files>
  <action>
    read_first:
    - web/src/pages/en/reviews/[slug].astro (review page pattern — no breadcrumbs, no ogImage)
    - web/src/pages/en/tutorials/[slug].astro (tutorial pattern — has breadcrumbs, no ogImage)
    - web/src/pages/en/blog/[slug].astro (blog pattern — has breadcrumbs, no ogImage)
    - web/src/pages/en/recipes/[slug].astro (recipe pattern — has ogImage, no breadcrumbs)
    - web/src/pages/en/index.astro (homepage — no breadcrumbs, no ogImage)
    - Read all IT and ES locale equivalents before editing them

    For EACH page type, apply these changes:

    **Review pages (en/it/es) — 3 files:**
    - Import `BreadcrumbJsonLd` from `@components/common/BreadcrumbJsonLd.astro`
    - Import `Breadcrumbs` from `@components/content/Breadcrumbs.astro` (not imported yet)
    - Add `breadcrumbItems` array: `[{ label: t('content.home'), href: '/{locale}/' }, { label: t('nav.reviews'), href: '/{locale}/reviews/' }, { label: review.title }]` (use localized route for it/es: `recensioni`/`resenas`)
    - Add `<Breadcrumbs items={breadcrumbItems} locale={locale} />` before the `<h1>` inside the article
    - Add `<BreadcrumbJsonLd items={breadcrumbItems} />` inside BaseLayout (after ReviewJsonLd)
    - Add `ogImage={coverImage || ''}` prop to `<BaseLayout>` (coverImage var already exists)

    **Tutorial pages (en/it/es) — 3 files:**
    - Import `BreadcrumbJsonLd` from `@components/common/BreadcrumbJsonLd.astro`
    - Add `<BreadcrumbJsonLd items={breadcrumbItems} />` (breadcrumbItems already defined)
    - Add `ogImage={coverImageUrl || ''}` prop to `<BaseLayout>` (coverImageUrl already exists)

    **Blog pages (en/it/es) — 3 files:**
    - Import `BreadcrumbJsonLd` from `@components/common/BreadcrumbJsonLd.astro`
    - Add `<BreadcrumbJsonLd items={breadcrumbItems} />` (breadcrumbItems already defined)
    - Add `ogImage={coverImageUrl || ''}` prop to `<BaseLayout>` (coverImageUrl already exists)

    **Recipe pages (en/it/es) — 3 files:**
    - Import `BreadcrumbJsonLd` from `@components/common/BreadcrumbJsonLd.astro`
    - Import `Breadcrumbs` from `@components/content/Breadcrumbs.astro` (if not imported)
    - Add `breadcrumbItems` array: `[{ label: t('content.home') or equivalent, href: '/{locale}/' }, { label: t('nav.recipes') or equivalent, href: '/{locale}/recipes/' }, { label: recipe.title }]` (use localized routes for it/es)
    - Add `<Breadcrumbs items={breadcrumbItems} locale={locale} />` before recipe title
    - Add `<BreadcrumbJsonLd items={breadcrumbItems} />` inside BaseLayout
    - ogImage already passed — no change needed

    **Homepage pages (en/it/es) — 3 files:**
    - No breadcrumbs needed (root page)
    - No BreadcrumbJsonLd needed (root page)
    - Add `ogImage="/images/og-default.jpg"` to BaseLayout (a default social preview image)
    - Note: The og-default.jpg file should be placed in `web/public/images/` — create a placeholder text file at that path with a comment noting it needs a real image. Or use the site URL as-is without image (empty string is already handled by SEOHead.astro conditionally).
    - Actually, since there's no real default image available, skip ogImage on homepage — the SEOHead already conditionally renders og:image only when ogImage is truthy, which is correct behavior.

    acceptance_criteria:
    - `grep -c "BreadcrumbJsonLd" web/src/pages/en/reviews/\[slug\].astro` returns >= 1
    - `grep -c "ogImage" web/src/pages/en/reviews/\[slug\].astro` returns >= 1
    - `grep -c "BreadcrumbJsonLd" web/src/pages/en/tutorials/\[slug\].astro` returns >= 1
    - `grep -c "ogImage" web/src/pages/en/tutorials/\[slug\].astro` returns >= 1
    - `grep -c "BreadcrumbJsonLd" web/src/pages/en/blog/\[slug\].astro` returns >= 1
    - `grep -c "ogImage" web/src/pages/en/blog/\[slug\].astro` returns >= 1
    - `grep -c "BreadcrumbJsonLd" web/src/pages/en/recipes/\[slug\].astro` returns >= 1
    - Same patterns hold for IT and ES locale pages
  </action>
  <verify>
    <automated>cd C:/Users/Matteo/Desktop/Progetti/bbqexperience/web && for f in src/pages/en/reviews/\[slug\].astro src/pages/en/tutorials/\[slug\].astro src/pages/en/blog/\[slug\].astro src/pages/en/recipes/\[slug\].astro; do echo "=== $f ===" && grep -c "BreadcrumbJsonLd" "$f" && grep -c "ogImage" "$f"; done</automated>
  </verify>
  <done>All 12 content detail pages (reviews, tutorials, blog, recipes x 3 locales) include BreadcrumbJsonLd JSON-LD and pass ogImage to BaseLayout. Visual breadcrumbs added to review and recipe pages that were missing them.</done>
</task>

</tasks>

<verification>
- `grep -r "BreadcrumbList" web/src/components/common/BreadcrumbJsonLd.astro` confirms JSON-LD type
- `grep -rl "BreadcrumbJsonLd" web/src/pages/` shows all pages importing the component
- `grep "i18n" web/astro.config.mjs` shows sitemap locale config
- `grep -rl "ogImage=" web/src/pages/` shows all pages passing ogImage
</verification>

<success_criteria>
- BreadcrumbJsonLd.astro generates valid BreadcrumbList Schema.org JSON-LD
- All 12 content detail pages (4 types x 3 locales) render BreadcrumbList JSON-LD
- All content pages with cover images pass ogImage to BaseLayout for OG/Twitter meta tags
- astro.config.mjs sitemap integration configured with i18n for en/it/es locale sitemaps
</success_criteria>

<output>
After completion, create `.planning/phases/09-seo-audit-performance-launch/09-01-SUMMARY.md`
</output>
