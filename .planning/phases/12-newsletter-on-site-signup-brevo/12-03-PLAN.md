---
phase: 12-newsletter-on-site-signup-brevo
plan: 03
type: execute
wave: 2
depends_on:
  - 12-01
files_modified:
  - web/src/pages/en/newsletter/index.astro
  - web/src/pages/it/newsletter/index.astro
  - web/src/pages/es/newsletter/index.astro
autonomous: false
requirements:
  - NEWS-02
  - NEWS-08

must_haves:
  truths:
    - "/en/newsletter/ returns 200 with hero, benefits grid, social proof, signup form, and consent text"
    - "/it/newsletter/ returns 200 with Italian copy"
    - "/es/newsletter/ returns 200 with Spanish copy"
    - "Each landing page has proper title, meta description, and hreflang to sibling locales"
    - "Signup form on landing page uses source=landing attribution"
    - "Brevo DOI templates and welcome automation are configured (manual)"
  artifacts:
    - path: "web/src/pages/en/newsletter/index.astro"
      provides: "EN newsletter landing page"
      contains: "NewsletterSignup"
    - path: "web/src/pages/it/newsletter/index.astro"
      provides: "IT newsletter landing page"
      contains: "NewsletterSignup"
    - path: "web/src/pages/es/newsletter/index.astro"
      provides: "ES newsletter landing page"
      contains: "NewsletterSignup"
  key_links:
    - from: "web/src/pages/en/newsletter/index.astro"
      to: "web/src/components/common/NewsletterSignup.astro"
      via: "Astro component with source=landing"
      pattern: "NewsletterSignup"
---

<objective>
Create the 3 newsletter landing pages (EN/IT/ES) with hero section, 3-column benefits grid, social proof, signup form (reusing NewsletterSignup), and consent text. Then verify Brevo manual setup (DOI templates, SURFACE attribute, welcome automation) via a human checkpoint.

Purpose: The dedicated landing page is the highest-intent signup surface and serves as the DOI confirmation redirect target. The Brevo admin checkpoint ensures the full DOI + welcome email flow works end-to-end.

Output: Three production-ready newsletter landing pages and confirmed Brevo DOI/welcome email configuration.
</objective>

<execution_context>
@C:/Users/Matteo/Desktop/Progetti/bbqexperience/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Matteo/Desktop/Progetti/bbqexperience/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-newsletter-on-site-signup-brevo/12-CONTEXT.md
@.planning/phases/12-newsletter-on-site-signup-brevo/12-RESEARCH.md
@.planning/phases/12-newsletter-on-site-signup-brevo/12-UI-SPEC.md
@.planning/phases/12-newsletter-on-site-signup-brevo/12-01-SUMMARY.md

<interfaces>
<!-- From Plan 01: upgraded NewsletterSignup + i18n keys -->
From web/src/components/common/NewsletterSignup.astro (after Plan 01):
```typescript
interface Props {
  translations: Record<string, any>;
  compact?: boolean;
  source?: 'inline' | 'landing' | 'footer' | 'exit-intent';
}
```

From web/src/i18n/en.json (Plan 01 added keys):
```json
"newsletter": {
  "landing": {
    "heroTitle": "Never Miss a Review",
    "heroSubtitle": "Join the BBQ Experience newsletter...",
    "benefit1Title": "Exclusive Reviews",
    "benefit1Desc": "...",
    "benefit2Title": "Original Recipes",
    "benefit2Desc": "...",
    "benefit3Title": "BBQ Secrets",
    "benefit3Desc": "...",
    "socialProof": "Join {count}+ pitmasters who get fire content every week",
    "consent": "By subscribing you agree to our {privacyLink}. We send one email per week. Unsubscribe anytime.",
    "privacyLinkText": "Privacy Policy",
    "metaTitle": "BBQ Newsletter - Exclusive Reviews, Recipes & Secrets",
    "metaDesc": "Subscribe to the BBQ Experience newsletter..."
  }
}
```

From web/src/lib/i18n.ts (Plan 01 added route):
```typescript
newsletter: { en: 'newsletter', it: 'newsletter', es: 'newsletter' },
```

Existing page pattern (from web/src/pages/en/blog/[slug].astro):
```typescript
import BaseLayout from '@layouts/BaseLayout.astro';
import Header from '@components/common/Header.astro';
import Footer from '@components/common/Footer.astro';
import SEOHead from '@components/common/SEOHead.astro';
import { loadTranslations, getTranslation } from '@lib/i18n';
import type { Locale } from '@lib/i18n';
export const prerender = false; // or true for static pages
```

Privacy page URL pattern: `/{locale}/privacy/`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 3 newsletter landing pages (EN/IT/ES)</name>
  <files>
    web/src/pages/en/newsletter/index.astro
    web/src/pages/it/newsletter/index.astro
    web/src/pages/es/newsletter/index.astro
  </files>
  <read_first>
    web/src/pages/en/blog/[slug].astro
    web/src/pages/en/privacy/index.astro
    web/src/components/common/SEOHead.astro
    web/src/components/common/Header.astro
    web/src/components/common/Footer.astro
    web/src/components/common/NewsletterSignup.astro
    web/src/layouts/BaseLayout.astro
    web/src/i18n/en.json
    web/src/i18n/it.json
    web/src/i18n/es.json
    web/src/lib/i18n.ts
  </read_first>
  <action>
Create 3 pages following the established page pattern. Each page is structurally identical, differing only in `locale` value. The privacy page (`web/src/pages/en/privacy/index.astro`) is the closest structural reference for a static content page.

**Astro frontmatter section** (the script block between the `--- ` delimiters at top of each .astro file):

```typescript
// Pagina dedicata newsletter ({LOCALE}) — landing page con proposta di valore e form iscrizione
import BaseLayout from '@layouts/BaseLayout.astro';
import Header from '@components/common/Header.astro';
import Footer from '@components/common/Footer.astro';
import SEOHead from '@components/common/SEOHead.astro';
import NewsletterSignup from '@components/common/NewsletterSignup.astro';
import { loadTranslations, getTranslation, getLocalizedPath } from '@lib/i18n';
import type { Locale } from '@lib/i18n';

export const prerender = true;

const locale: Locale = 'en';  // 'it' for IT page, 'es' for ES page
const translations = await loadTranslations(locale);
const t = (key: string) => getTranslation(translations, key);

const seoTitle = t('newsletter.landing.metaTitle');
const seoDescription = t('newsletter.landing.metaDesc');
const canonicalUrl = `https://bbq-experience.com/${locale}/newsletter/`;
```

**Template section** (the HTML part after the frontmatter):

```html
<BaseLayout locale={locale} translations={translations}>
  <SEOHead
    slot="head"
    title={seoTitle}
    description={seoDescription}
    url={canonicalUrl}
    locale={locale}
  />

  <Header slot="header" locale={locale} translations={translations} currentPath={Astro.url.pathname} />

  <!-- Hero -->
  <section class="nl-hero">
    <div class="container-bbq">
      <h1 class="nl-hero__title">{t('newsletter.landing.heroTitle')}</h1>
      <p class="nl-hero__subtitle">{t('newsletter.landing.heroSubtitle')}</p>
    </div>
  </section>

  <!-- Benefici: 3-column grid -->
  <section class="nl-benefits">
    <div class="container-bbq">
      <div class="nl-benefits__grid">
        <div class="nl-benefits__card">
          <svg class="nl-benefits__icon" width="48" height="48" viewBox="0 0 48 48" fill="none" aria-hidden="true">
            <path d="M24 4C20 10 14 16 14 24c0 5.523 4.477 10 10 10s10-4.477 10-10C34 16 28 10 24 4z" fill="var(--color-accent-fire)"/>
          </svg>
          <h3 class="nl-benefits__title">{t('newsletter.landing.benefit1Title')}</h3>
          <p class="nl-benefits__desc">{t('newsletter.landing.benefit1Desc')}</p>
        </div>
        <div class="nl-benefits__card">
          <svg class="nl-benefits__icon" width="48" height="48" viewBox="0 0 48 48" fill="none" aria-hidden="true">
            <path d="M24 6c-2 4-6 6-6 12a6 6 0 0012 0c0-6-4-8-6-12zM12 28h24v4c0 6.627-5.373 12-12 12S12 38.627 12 32v-4z" fill="var(--color-accent-fire)"/>
          </svg>
          <h3 class="nl-benefits__title">{t('newsletter.landing.benefit2Title')}</h3>
          <p class="nl-benefits__desc">{t('newsletter.landing.benefit2Desc')}</p>
        </div>
        <div class="nl-benefits__card">
          <svg class="nl-benefits__icon" width="48" height="48" viewBox="0 0 48 48" fill="none" aria-hidden="true">
            <rect x="10" y="20" width="28" height="22" rx="3" fill="var(--color-accent-fire)" opacity="0.2"/>
            <path d="M24 20V12m0 0l-6 6m6-6l6 6" stroke="var(--color-accent-fire)" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"/>
            <rect x="10" y="20" width="28" height="22" rx="3" stroke="var(--color-accent-fire)" stroke-width="2" fill="none"/>
          </svg>
          <h3 class="nl-benefits__title">{t('newsletter.landing.benefit3Title')}</h3>
          <p class="nl-benefits__desc">{t('newsletter.landing.benefit3Desc')}</p>
        </div>
      </div>
    </div>
  </section>

  <!-- Social proof -->
  <section class="nl-social">
    <div class="container-bbq">
      <p class="nl-social__text" set:html={t('newsletter.landing.socialProof').replace('{count}', '<span class="nl-social__count">74,000</span>')} />
    </div>
  </section>

  <!-- Form iscrizione -->
  <section class="nl-signup">
    <div class="container-bbq">
      <div class="nl-signup__wrapper">
        <NewsletterSignup translations={translations} source="landing" />
      </div>
    </div>
  </section>

  <!-- Testo consenso -->
  <section class="nl-consent">
    <div class="container-bbq">
      <p class="nl-consent__text" set:html={t('newsletter.landing.consent').replace('{privacyLink}', `<a href="/${locale}/privacy/" class="nl-consent__link">${t('newsletter.landing.privacyLinkText')}</a>`)} />
    </div>
  </section>

  <Footer slot="footer" locale={locale} translations={translations} />
</BaseLayout>
```

**Scoped styles:**

```css
.nl-hero { padding: var(--space-16) var(--space-4); text-align: center; }
.nl-hero__title { font-family: var(--font-heading); font-size: var(--text-3xl); font-weight: 700; text-transform: uppercase; letter-spacing: 0.02em; color: var(--color-text-primary); margin: 0; }
.nl-hero__subtitle { font-size: var(--text-base); color: var(--color-text-secondary); max-width: 640px; margin: var(--space-4) auto 0; line-height: 1.5; }
.nl-benefits { padding: var(--space-12) var(--space-4); background: var(--color-bg-secondary); }
.nl-benefits__grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(280px, 1fr)); gap: var(--space-6); }
.nl-benefits__card { padding: var(--space-6); text-align: center; }
.nl-benefits__icon { margin: 0 auto; }
.nl-benefits__title { font-family: var(--font-heading); font-size: var(--text-base); font-weight: 700; text-transform: uppercase; color: var(--color-text-primary); margin: var(--space-3) 0 0; }
.nl-benefits__desc { font-size: var(--text-sm); color: var(--color-text-muted); line-height: 1.5; margin: var(--space-2) 0 0; }
.nl-social { padding: var(--space-8) var(--space-4); text-align: center; }
.nl-social__text { font-size: var(--text-2xl); font-weight: 700; color: var(--color-text-secondary); margin: 0; }
.nl-social__count { color: var(--color-accent-fire); }
.nl-signup { padding: var(--space-12) var(--space-4); text-align: center; }
.nl-signup__wrapper { max-width: 560px; margin: 0 auto; }
.nl-consent { padding: 0 var(--space-4) var(--space-12); text-align: center; }
.nl-consent__text { font-size: var(--text-sm); color: var(--color-text-muted); line-height: 1.5; max-width: 480px; margin: 0 auto; }
.nl-consent__link { color: var(--color-accent-fire); text-decoration: none; }
.nl-consent__link:hover { text-decoration: underline; }
```

**For IT page** (`web/src/pages/it/newsletter/index.astro`):
- Change `const locale: Locale = 'it';`
- Change social proof count format to `74.000` (Italian number format -- dot instead of comma)
- Everything else identical (all copy comes from i18n keys)

**For ES page** (`web/src/pages/es/newsletter/index.astro`):
- Change `const locale: Locale = 'es';`
- Social proof count `74.000` (Spanish uses dot for thousands too)
- Everything else identical

**D-19 SEO:** The `SEOHead` component already handles `<title>`, `<meta name="description">`, and hreflang links to sibling locales. No JSON-LD needed per decision.
  </action>
  <verify>
    <automated>cd web && test -f src/pages/en/newsletter/index.astro && test -f src/pages/it/newsletter/index.astro && test -f src/pages/es/newsletter/index.astro && echo "3 pages exist"</automated>
  </verify>
  <acceptance_criteria>
    - web/src/pages/en/newsletter/index.astro exists and contains source="landing"
    - web/src/pages/it/newsletter/index.astro exists and contains locale: Locale = 'it'
    - web/src/pages/es/newsletter/index.astro exists and contains locale: Locale = 'es'
    - All 3 pages contain SEOHead with title and description from i18n keys
    - All 3 pages contain NewsletterSignup with source="landing"
    - All 3 pages contain privacy policy link (/{locale}/privacy/)
    - All 3 pages contain social proof section with 74,000 or 74.000
    - All 3 pages contain 3 benefit cards (benefit1, benefit2, benefit3)
    - EN page contains newsletter.landing.heroTitle key reference
    - All 3 pages use BaseLayout, Header, Footer components
  </acceptance_criteria>
  <done>Three newsletter landing pages (EN/IT/ES) created with hero, 3-column benefits grid, social proof, signup form with source="landing", and consent text with privacy link. SEO meta and hreflang handled by SEOHead.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify Brevo DOI templates, SURFACE attribute, and welcome automation</name>
  <files>n/a</files>
  <action>
This is a manual verification checkpoint. Matteo must configure Brevo dashboard settings and verify the end-to-end DOI flow. No code changes needed.

Steps:
1. Create SURFACE contact attribute in Brevo (text type)
2. Create 3 DOI email templates (EN/IT/ES) in Brevo Transactional
3. Set env vars BREVO_DOI_TEMPLATE_EN/IT/ES with template IDs
4. Create welcome email automation triggered by "Contact added to list"
5. Test end-to-end flow with a real email submission
  </action>
  <verify>
    <automated>echo "Manual verification - Brevo admin panel configuration"</automated>
  </verify>
  <what-built>
    All 4 newsletter signup surfaces are coded and deployed:
    1. Inline form at end of every article (12 pages)
    2. Exit-intent modal on desktop
    3. Sticky footer bar
    4. Dedicated /newsletter landing pages (EN/IT/ES)

    The API now calls Brevo DOI endpoint with templateId per locale. For this to work, Brevo must be configured.
  </what-built>
  <how-to-verify>
    **Brevo dashboard setup (D-24, D-25, D-26):**

    1. **SURFACE attribute:** Go to Brevo -> Contacts -> Settings -> Contact Attributes. Verify "SURFACE" exists as a text attribute. If not, create it.

    2. **DOI templates:** Go to Brevo -> Transactional -> Templates. Create 3 DOI confirmation email templates (EN/IT/ES) if not already done. Note their numeric template IDs.

    3. **Env vars:** Update the server `.env` with:
       - `BREVO_DOI_TEMPLATE_EN=<template_id>`
       - `BREVO_DOI_TEMPLATE_IT=<template_id>`
       - `BREVO_DOI_TEMPLATE_ES=<template_id>`
       Also update Dockerfile ARG lines and docker run -e flags.

    4. **Welcome automation:** Go to Brevo -> Automations. Create an automation triggered by "Contact added to list" (your newsletter list). Send locale-matched welcome email. This fires ONLY after DOI confirmation.

    5. **End-to-end test:** After deploy, visit https://bbq-experience.com/en/newsletter/ and submit a test email. Verify:
       - Form shows "Check your inbox" confirmation message
       - Confirmation email arrives from Brevo
       - After clicking confirm link, redirects to /en/newsletter/?confirmed=true
       - Welcome email arrives
       - Brevo contact shows SURFACE="landing" and LOCALE="en"
       - Strapi subscriber shows source="landing"

    Type "approved" when everything works, or describe issues.
  </how-to-verify>
  <done>Brevo DOI templates configured for EN/IT/ES, SURFACE attribute created, welcome automation active, end-to-end signup flow verified with test email.</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Landing page form -> /api/newsletter | Same boundary as inline form; no additional surface |
| Brevo DOI redirect -> /newsletter/?confirmed=true | User clicks link in email; no untrusted input (Brevo controls the redirect URL) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-10 | Spoofing | Brevo DOI redirect URL manipulation | accept | redirectionUrl is hardcoded server-side in API; user cannot modify it from the form |
| T-12-11 | Information Disclosure | BREVO_DOI_TEMPLATE_* env vars | mitigate | Template IDs are numeric (not secret), but stored in .env (not committed). Dockerfile uses ARG per CLAUDE.md convention |
</threat_model>

<verification>
1. `curl -s -o /dev/null -w "%{http_code}" https://bbq-experience.com/en/newsletter/` returns 200
2. `curl -s -o /dev/null -w "%{http_code}" https://bbq-experience.com/it/newsletter/` returns 200
3. `curl -s -o /dev/null -w "%{http_code}" https://bbq-experience.com/es/newsletter/` returns 200
4. Each page contains source="landing" in HTML source
5. Brevo test subscription completes DOI flow end-to-end
</verification>

<success_criteria>
- Three newsletter landing pages serve 200 in all locales with full content sections
- Brevo DOI templates exist for EN/IT/ES with correct template IDs in env vars
- SURFACE contact attribute configured in Brevo
- Welcome automation fires on DOI confirmation
- End-to-end signup flow works: form submit -> confirm email -> click link -> welcome email -> contact in Brevo with SURFACE + LOCALE attributes
</success_criteria>

<output>
After completion, create `.planning/phases/12-newsletter-on-site-signup-brevo/12-03-SUMMARY.md`
</output>
