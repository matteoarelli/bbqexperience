---
phase: 14-recipe-collections
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cms/src/api/recipe-collection/content-types/recipe-collection/schema.json
  - web/src/lib/types.ts
  - web/src/lib/i18n.ts
  - web/src/i18n/en.json
  - web/src/i18n/it.json
  - web/src/i18n/es.json
  - web/src/i18n/types.ts
  - web/src/components/collection/CollectionCard.astro
  - web/src/components/collection/CollectionBadge.astro
autonomous: true
requirements:
  - COLL-01
must_haves:
  truths:
    - "recipe-collection schema includes description and author_note fields with i18n localized"
    - "StrapiRecipeCollection TypeScript interface exists in types.ts"
    - "'recipe-collections' is part of ContentType union"
    - "'collections' route exists in localizedRoutes with en/it/es slugs"
    - "All 3 locale JSON files have collections.* translation keys"
    - "CollectionCard and CollectionBadge components exist"
  artifacts:
    - path: "cms/src/api/recipe-collection/content-types/recipe-collection/schema.json"
      provides: "Schema with description, author_note fields"
      contains: "author_note"
    - path: "web/src/lib/types.ts"
      provides: "StrapiRecipeCollection interface + ContentType union update"
      contains: "StrapiRecipeCollection"
    - path: "web/src/lib/i18n.ts"
      provides: "collections route in localizedRoutes"
      contains: "raccolte"
    - path: "web/src/components/collection/CollectionCard.astro"
      provides: "Card component for collection listing"
    - path: "web/src/components/collection/CollectionBadge.astro"
      provides: "Badge component for recipe detail pages"
  key_links:
    - from: "web/src/lib/types.ts"
      to: "cms schema.json"
      via: "StrapiRecipeCollection mirrors schema fields"
      pattern: "StrapiRecipeCollection"
    - from: "web/src/lib/i18n.ts"
      to: "page routes"
      via: "localizedRoutes.collections"
      pattern: "collections.*raccolte.*colecciones"
---

<objective>
Foundation: Strapi schema evolution, TypeScript types, i18n keys, and reusable components for recipe collections.

Purpose: Establish all contracts, types, translations, and components that the page-level plans (02, 03) will consume. By defining interfaces first, downstream executors work against concrete types rather than guessing.

Output: Updated CMS schema, TS types, 3 locale JSON files, and 2 Astro components (CollectionCard, CollectionBadge).
</objective>

<execution_context>
@C:/Users/Matteo/Desktop/Progetti/bbqexperience/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Matteo/Desktop/Progetti/bbqexperience/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-recipe-collections/14-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From web/src/lib/types.ts (current ContentType union):
```typescript
export type ContentType =
  | 'reviews' | 'recipes' | 'tutorials' | 'blog-posts'
  | 'products' | 'instagram-posts' | 'brands' | 'partnerships'
  | 'keyword-trackers' | 'content-queues' | 'product-categories';
```

From web/src/lib/types.ts (StrapiMedia, StrapiEntity used by new interface):
```typescript
export interface StrapiMedia { id: number; name: string; url: string; alternativeText: string | null; width: number; height: number; formats: { thumbnail?: StrapiImageFormat; small?: StrapiImageFormat; medium?: StrapiImageFormat; large?: StrapiImageFormat; } | null; }
export interface StrapiEntity { id: number; documentId: string; createdAt: string; updatedAt: string; publishedAt: string | null; locale: string; }
```

From web/src/lib/i18n.ts (localizedRoutes pattern):
```typescript
export const localizedRoutes: Record<string, Record<Locale, string>> = {
  reviews: { en: 'reviews', it: 'recensioni', es: 'resenas' },
  recipes: { en: 'recipes', it: 'ricette', es: 'recetas' },
  // ... more routes
};
```

From web/src/i18n/types.ts (auto-derived from en.json):
```typescript
export type TranslationKey = NestedKeyOf<typeof en>;
```

From cms schema.json (current recipe-collection fields):
```json
{
  "attributes": {
    "title": { "type": "string", "required": true, "pluginOptions": { "i18n": { "localized": true } } },
    "slug": { "type": "uid", "targetField": "title" },
    "hero_image": { "type": "media", "multiple": false, "allowedTypes": ["images"] },
    "editorial_intro": { "type": "richtext", "pluginOptions": { "i18n": { "localized": true } } },
    "order": { "type": "integer", "default": 0 },
    "recipes": { "type": "relation", "relation": "oneToMany", "target": "api::recipe.recipe", "mappedBy": "collection" }
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Evolve Strapi schema + TypeScript types + i18n routes</name>
  <files>
    cms/src/api/recipe-collection/content-types/recipe-collection/schema.json,
    web/src/lib/types.ts,
    web/src/lib/i18n.ts,
    web/src/i18n/en.json,
    web/src/i18n/it.json,
    web/src/i18n/es.json
  </files>
  <read_first>
    cms/src/api/recipe-collection/content-types/recipe-collection/schema.json,
    web/src/lib/types.ts,
    web/src/lib/i18n.ts,
    web/src/i18n/en.json,
    web/src/i18n/it.json,
    web/src/i18n/es.json,
    web/src/i18n/types.ts
  </read_first>
  <action>
1. **Schema evolution** — Add two fields to `cms/src/api/recipe-collection/content-types/recipe-collection/schema.json` attributes:
   ```json
   "description": {
     "type": "text",
     "pluginOptions": { "i18n": { "localized": true } }
   },
   "author_note": {
     "type": "richtext",
     "pluginOptions": { "i18n": { "localized": true } }
   }
   ```
   Place them after `editorial_intro` and before `order`.

2. **ContentType union** — In `web/src/lib/types.ts`, add `| 'recipe-collections'` to the ContentType union (after `'product-categories'`).

3. **StrapiRecipeCollection interface** — Add after the StrapiProduct interface block in `web/src/lib/types.ts`:
   ```typescript
   /** Raccolta tematica di ricette BBQ */
   export interface StrapiRecipeCollection {
     title: string;
     slug: string;
     description: string | null;
     editorial_intro: string | null;
     author_note: string | null;
     hero_image: StrapiMedia | null;
     order: number;
     recipes: (StrapiRecipe & StrapiEntity)[] | null;
     localizations?: { locale: string; publishedAt: string | null; slug: string }[];
   }
   ```

4. **localizedRoutes** — In `web/src/lib/i18n.ts`, add to the `localizedRoutes` object (after `newsletter`):
   ```typescript
   collections: { en: 'collections', it: 'raccolte', es: 'colecciones' },
   ```

5. **Translation keys** — Add a `"collections"` block to all 3 locale JSON files:

   en.json:
   ```json
   "collections": {
     "title": "Recipe Collections",
     "allCollections": "All Collections",
     "recipesCount": "{count} recipes",
     "partOf": "Part of",
     "authorsNote": "Author's Note",
     "emptyState": "No collections available yet.",
     "viewCollection": "View Collection",
     "description": "Curated recipe collections by our pitmasters"
   }
   ```

   it.json:
   ```json
   "collections": {
     "title": "Raccolte di Ricette",
     "allCollections": "Tutte le Raccolte",
     "recipesCount": "{count} ricette",
     "partOf": "Parte di",
     "authorsNote": "Nota dell'Autore",
     "emptyState": "Nessuna raccolta disponibile al momento.",
     "viewCollection": "Vedi Raccolta",
     "description": "Raccolte di ricette curate dai nostri pitmaster"
   }
   ```

   es.json:
   ```json
   "collections": {
     "title": "Colecciones de Recetas",
     "allCollections": "Todas las Colecciones",
     "recipesCount": "{count} recetas",
     "partOf": "Parte de",
     "authorsNote": "Nota del Autor",
     "emptyState": "No hay colecciones disponibles por el momento.",
     "viewCollection": "Ver Coleccion",
     "description": "Colecciones de recetas curadas por nuestros pitmasters"
   }
   ```

   Note: the `types.ts` file auto-derives TranslationKey from en.json, so no manual update needed there.

6. **Nav update** — Add `"collections": "Collections"` to `nav` object in en.json, `"collections": "Raccolte"` in it.json, `"collections": "Colecciones"` in es.json.
  </action>
  <verify>
    <automated>cd "C:/Users/Matteo/Desktop/Progetti/bbqexperience/web" && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep "recipe-collections" web/src/lib/types.ts returns a match in ContentType union
    - grep "StrapiRecipeCollection" web/src/lib/types.ts returns the interface definition
    - grep "author_note" cms/src/api/recipe-collection/content-types/recipe-collection/schema.json returns a match
    - grep "description" cms/src/api/recipe-collection/content-types/recipe-collection/schema.json returns a match (the new text field)
    - grep "raccolte" web/src/lib/i18n.ts returns a match in localizedRoutes
    - grep "collections" web/src/i18n/en.json returns matches for collection translation keys
    - grep "partOf" web/src/i18n/en.json returns "Part of"
    - grep "Raccolte" web/src/i18n/it.json returns matches
    - grep "Colecciones" web/src/i18n/es.json returns matches
  </acceptance_criteria>
  <done>Schema has description + author_note fields, TypeScript types compile, localizedRoutes includes collections, all 3 locale files have collections.* keys</done>
</task>

<task type="auto">
  <name>Task 2: Create CollectionCard and CollectionBadge components</name>
  <files>
    web/src/components/collection/CollectionCard.astro,
    web/src/components/collection/CollectionBadge.astro
  </files>
  <read_first>
    web/src/components/home/RecipeCard.astro,
    web/src/lib/media.ts,
    web/src/lib/i18n.ts,
    web/src/lib/types.ts
  </read_first>
  <action>
1. **CollectionCard.astro** — Create `web/src/components/collection/CollectionCard.astro` following the RecipeCard pattern:
   - Props: `collection: StrapiRecipeCollection & StrapiEntity`, `locale: Locale`, `collectionsRoute: string`, `translations: Record<string, any>`
   - Render: hero_image via `getStrapiMediaURL()` with responsive srcset via `buildStrapiSrcset()`, title as h3, recipe count badge (using `collection.recipes?.length || 0`), description text (truncated to 120 chars if needed)
   - Link: wraps entire card in `<a href="/{locale}/{collectionsRoute}/{collection.slug}/">`
   - Style: follow RecipeCard grid styling (same border, radius, hover shadow, dark bg pattern). Use `var(--color-bg-secondary)` background, `var(--color-border-subtle)` border, `var(--font-heading)` for title
   - Recipe count badge: use `var(--color-accent-fire)` background, white text, rounded pill, positioned top-right of image
   - Import `getStrapiMediaURL`, `buildStrapiSrcset` from `@lib/media`, `getTranslation` from `@lib/i18n`, types from `@lib/types`

2. **CollectionBadge.astro** — Create `web/src/components/collection/CollectionBadge.astro`:
   - Props: `collectionTitle: string`, `collectionSlug: string`, `locale: Locale`, `translations: Record<string, any>`
   - Compute `collectionsRoute` from `localizedRoutes.collections[locale]`
   - Render: inline element with fire icon (svg or unicode fire emoji), "{partOf} {collectionTitle}" text as a link to `/{locale}/{collectionsRoute}/{collectionSlug}/`
   - Style: `display: inline-flex`, `align-items: center`, `gap: var(--space-2)`, `padding: var(--space-2) var(--space-4)`, `border-radius: var(--radius-full)`, background `var(--color-accent-fire)` with opacity 0.1, text color `var(--color-accent-fire)`, `font-family: var(--font-heading)`, `font-size: var(--text-sm)`, `font-weight: 600`, `text-transform: uppercase`, `letter-spacing: 0.04em`. Hover: background opacity increases to 0.2
   - Import `localizedRoutes, getTranslation` from `@lib/i18n`, type `Locale` from `@lib/i18n`
  </action>
  <verify>
    <automated>cd "C:/Users/Matteo/Desktop/Progetti/bbqexperience/web" && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - File web/src/components/collection/CollectionCard.astro exists and contains "getStrapiMediaURL"
    - File web/src/components/collection/CollectionBadge.astro exists and contains "localizedRoutes.collections"
    - grep "StrapiRecipeCollection" web/src/components/collection/CollectionCard.astro returns a match
    - grep "collectionSlug" web/src/components/collection/CollectionBadge.astro returns a match
    - grep "partOf" web/src/components/collection/CollectionBadge.astro returns a match
    - TypeScript compilation passes (npx tsc --noEmit exits 0)
  </acceptance_criteria>
  <done>CollectionCard renders collection cover/title/count/description as a linked card. CollectionBadge renders "Part of [X]" pill linking to collection. Both compile without TS errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Strapi API -> Astro SSR | CMS content rendered in templates; untrusted richtext |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-14-01 | Tampering | editorial_intro / author_note richtext | mitigate | All richtext passes through renderMarkdown() which sanitizes; Astro auto-escapes non-set:html |
| T-14-02 | Tampering | slug in URL params | mitigate | fetchBySlug returns null for invalid slugs -> 404 response |
</threat_model>

<verification>
- `npx tsc --noEmit` passes in web/ directory
- Schema JSON is valid (parseable with `node -e "JSON.parse(require('fs').readFileSync('cms/src/api/recipe-collection/content-types/recipe-collection/schema.json','utf8'))"`)
- grep confirms all new fields, types, routes, and translations exist
</verification>

<success_criteria>
- CMS schema ready for deploy with description + author_note
- TypeScript types define StrapiRecipeCollection matching schema
- ContentType union includes 'recipe-collections'
- localizedRoutes has collections: en/it/es
- 3 locale JSONs have all collections.* keys
- CollectionCard + CollectionBadge components exist and compile
</success_criteria>

<output>
After completion, create `.planning/phases/14-recipe-collections/14-01-SUMMARY.md`
</output>
